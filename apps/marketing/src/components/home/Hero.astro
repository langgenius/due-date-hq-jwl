---
/**
 * Homepage hero (production-v2 design, ported to Astro on the --m-* token tier).
 * Copy is welcoming-but-restrained ("a calm expert who's got your back").
 * English inline for now; i18n wrapping happens in the localization pass.
 * The Alerts panel is the signature surface — static markup here; the
 * filter/expand/apply micro-interactions layer on in a follow-up.
 */
interface Props {
  locale?: 'en' | 'zh-CN'
  ctaHref: string
  workflowAnchor?: string
}
const { locale = 'en', ctaHref, workflowAnchor = '#how' } = Astro.props
const zh = locale === 'zh-CN'

const t = zh
  ? {
      headBefore: '截止日刚刚变了。你知道它影响到',
      headItal: '谁',
      headAfter: '吗？',
      subLead: 'DueDateHQ 全天候监控 IRS、各州税务机关和 FEMA ——',
      subEm: '日期一变动，',
      subRest: '你立刻看到它影响到哪些客户，每条变更都附官方来源。',
      subStrong: '粘贴你的客户名单，约 10 分钟后第一条带来源的截止日就会出现。',
      points: [
        { strong: '我们全天候盯着 IRS、主要州税务机关和 FEMA', rest: '' },
        { strong: '你能看到', rest: '每次变化具体影响到哪些客户' },
        { strong: '每个日期都可追溯', rest: '回它的官方来源' },
        { strong: 'beta 期间免费', rest: '—— 无需信用卡' },
      ],
      ctaStart: '免费开始',
      ctaHow: '看它如何运作',
      panelAria: '本周示例提醒',
      panelTitleAlerts: '提醒',
      panelTitleWhen: '本周',
      panelCount: '6 项变化 · 31 个客户',
      panelCap: '实时预览 · 非你的真实数据',
      filterAria: '按辖区筛选',
      filters: ['全部', '联邦', 'CA', 'TX', 'NY'],
      sevUrgent: '紧急',
      sevInfo: '信息',
      affectsLabel: '影响',
      alerts: [
        {
          sev: 'urgent',
          juris: 'FED',
          source: 'irs.gov',
          title: 'IRS 在灾害声明后推迟申报与缴税截止日',
          from: 'Apr 15',
          to: 'Nov 3, 2026',
          delta: '推迟 202 天',
          affects: '8 个客户',
          status: '6 项就绪 · 2 项待复核',
        },
        {
          sev: 'info',
          juris: 'CA',
          source: 'ftb.ca.gov',
          title: '加州 FTB 在山火声明后延长第三季度预估税截止日',
          from: 'Sep 15',
          to: 'Sep 29, 2026',
          delta: '推迟 14 天',
          affects: '12 个客户',
          status: '9 项就绪 · 3 项待复核',
        },
        {
          sev: 'info',
          juris: 'TX',
          source: 'comptroller.texas.gov',
          title: '德州对远程卖家新增特许经营税申报要求',
          effective: '2026 年 1 月 1 日起生效',
          badge: '新义务',
          affects: '5 个客户',
          status: '5 项就绪',
        },
      ],
    }
  : {
      headBefore: 'A deadline just moved. Do you know',
      headItal: 'who',
      headAfter: 'it hits?',
      subLead: 'DueDateHQ watches the IRS, every state, and FEMA around the clock —',
      subEm: 'the moment a date moves,',
      subRest: 'you see exactly which clients it affects, with the source on every change.',
      subStrong:
        'Paste your client list and your first sourced deadline shows up in about 10 minutes.',
      points: [
        { strong: 'We watch the IRS, major state agencies & FEMA', rest: '— around the clock' },
        { strong: 'You see the exact clients', rest: 'each change touches' },
        { strong: 'Every date links back', rest: 'to its official source' },
        { strong: 'Free during the beta', rest: '— no card required' },
      ],
      ctaStart: 'Start free',
      ctaHow: 'See how it works',
      panelAria: 'Sample alerts for this week',
      panelTitleAlerts: 'Alerts',
      panelTitleWhen: 'this week',
      panelCount: '6 changes · 31 clients',
      panelCap: 'live preview · not your data',
      filterAria: 'Filter by jurisdiction',
      filters: ['All', 'Federal', 'CA', 'TX', 'NY'],
      sevUrgent: 'URGENT',
      sevInfo: 'INFO',
      affectsLabel: 'Affects',
      alerts: [
        {
          sev: 'urgent',
          juris: 'FED',
          source: 'irs.gov',
          title: 'IRS postpones filing & payment deadlines after disaster declaration',
          from: 'Apr 15',
          to: 'Nov 3, 2026',
          delta: '202 days later',
          affects: '8 clients',
          status: '6 ready · 2 review',
        },
        {
          sev: 'info',
          juris: 'CA',
          source: 'ftb.ca.gov',
          title: 'California FTB extends the Q3 estimated-tax deadline after wildfire declaration',
          from: 'Sep 15',
          to: 'Sep 29, 2026',
          delta: '14 days later',
          affects: '12 clients',
          status: '9 ready · 3 review',
        },
        {
          sev: 'info',
          juris: 'TX',
          source: 'comptroller.texas.gov',
          title: 'Texas adds a franchise-tax filing requirement for remote sellers',
          effective: 'Effective Jan 1, 2026',
          badge: 'new obligation',
          affects: '5 clients',
          status: '5 ready',
        },
      ],
    }
const points = t.points
const alerts = t.alerts
---

<section class="hero" aria-labelledby="hero-h" data-zh={zh ? '' : undefined}>
  <div class="hero__inner">
    <div class="hero__copy">
      <h1 class="hero__head" id="hero-h">
        {t.headBefore} <span class="ital">{t.headItal}</span> {t.headAfter}
      </h1>
      <p class="hero__sub">
        {t.subLead} <em>{t.subEm}</em> {t.subRest}
      </p>

      <ul class="hero__points">
        {
          points.map((p) => (
            <li>
              <svg viewBox="0 0 24 24" fill="none" aria-hidden="true">
                <circle cx="12" cy="12" r="9" stroke="currentColor" stroke-width="1.6" />
                <path
                  d="M8.5 12.2l2.4 2.3 4.6-4.8"
                  stroke="currentColor"
                  stroke-width="1.8"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                />
              </svg>
              <span>
                <b>{p.strong}</b> {p.rest}
              </span>
            </li>
          ))
        }
      </ul>

      <div class="hero__cta">
        <a class="btn btn--primary" href={ctaHref} data-event="marketing.hero.cta">
          {t.ctaStart} <span class="arrow" aria-hidden="true">→</span>
        </a>
        <a class="btn btn--ghost" href={workflowAnchor}>{t.ctaHow}</a>
      </div>
      <p class="hero__reassure">{t.subStrong}</p>
    </div>

    <!-- signature surface: this week's alerts (live preview · not your data) -->
    <div class="panel" aria-label={t.panelAria}>
      <div class="panel__head">
        <span class="panel__title">
          <span class="panel__live" aria-hidden="true"></span>{t.panelTitleAlerts}<span
            class="panel__dot">·</span
          >{t.panelTitleWhen}
        </span>
        <span class="panel__count">{t.panelCount}</span>
        <span class="panel__cap">{t.panelCap}</span>
      </div>

      <div class="panel__filter" aria-hidden="true">
        {
          t.filters.map((j, i) => (
            <span class={`chip${i === 0 ? ' chip--on' : ''}`}>{j}</span>
          ))
        }
      </div>

      <div class="panel__rows">
        {
          alerts.map((a) => (
            <article class={`row${a.sev === 'urgent' ? ' row--anchor' : ' row--secondary'}`}>
              <div class="row__top">
                <span class={`sev sev--${a.sev}`}>{a.sev === 'urgent' ? t.sevUrgent : t.sevInfo}</span>
                <span class="juris">{a.juris}</span>
                <span class="src">
                  <svg viewBox="0 0 24 24" fill="none" aria-hidden="true">
                    <path
                      d="M10 14a5 5 0 007.5.5l3-3a5 5 0 00-7-7l-1.7 1.7M14 10a5 5 0 00-7.5-.5l-3 3a5 5 0 007 7l1.7-1.7"
                      stroke="currentColor"
                      stroke-width="1.6"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                    />
                  </svg>
                  {a.source}
                </span>
              </div>
              <p class="row__title">{a.title}</p>
              {a.delta ? (
                <p class="row__change">
                  <s>{a.from}</s> <span class="arrow">→</span> <b>{a.to}</b>
                  <span class="row__delta row__delta--later">
                    {a.delta}
                  </span>
                </p>
              ) : (
                <p class="row__change">
                  <b>{a.effective}</b>
                  <span class="row__badge">{a.badge}</span>
                </p>
              )}
              <p class="row__affect">
                <svg viewBox="0 0 24 24" fill="none" aria-hidden="true">
                  <path
                    d="M17 20v-1a4 4 0 00-4-4H7a4 4 0 00-4 4v1M10 11a3.5 3.5 0 100-7 3.5 3.5 0 000 7M21 20v-1a4 4 0 00-3-3.9"
                    stroke="currentColor"
                    stroke-width="1.5"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                  />
                </svg>
                {t.affectsLabel} <b>{a.affects}</b>
                {a.sev === 'urgent' ? <span class="row__status">{a.status}</span> : null}
              </p>
            </article>
          ))
        }
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    max-width: var(--m-maxw);
    margin-inline: auto;
    padding-inline: var(--m-gutter);
    padding-top: clamp(56px, 6.5vw, 108px);
    padding-bottom: clamp(80px, 10vw, 132px);
  }
  .hero__inner {
    display: grid;
    grid-template-columns: repeat(12, minmax(0, 1fr));
    column-gap: clamp(36px, 4.5vw, 76px);
    gap: clamp(40px, 5vw, 64px);
    align-items: center;
  }
  .hero__copy {
    grid-column: span 6;
    align-self: center;
  }
  .panel {
    grid-column: span 6;
  }

  /* ---- copy ---- */
  .hero__head {
    font-family: var(--m-font-display);
    font-weight: 400;
    /* Bigger, more confident ceiling: real impact at 1440–1920, graceful 40px floor on mobile. */
    font-size: clamp(40px, 5.4vw, 76px);
    line-height: 1.02;
    letter-spacing: -0.02em;
    color: var(--m-ink);
    max-width: 15ch;
    text-wrap: balance;
  }
  .hero__head .ital {
    font-style: italic;
    /* Italic display cut reads tighter — pull a hair of optical spacing back. */
    letter-spacing: 0.002em;
  }
  /* CJK glyphs are full-width and heavier — the Latin ceiling overwhelms the column.
     Cap the size, widen the measure for a clean 2–3 lines, and drop the tight tracking. */
  .hero[data-zh] .hero__head {
    font-size: clamp(34px, 4vw, 56px);
    line-height: 1.18;
    letter-spacing: 0;
    max-width: 18ch;
  }
  .hero__sub {
    margin-top: clamp(22px, 1.7vw, 28px);
    max-width: 53ch;
    font-size: clamp(15px, 1.05vw, 16.5px);
    line-height: 1.6;
    color: var(--m-ink-2);
  }
  .hero__sub em {
    font-style: italic;
    color: var(--m-ink);
  }
  /* zh: CJK has no italic — the synthesized oblique reads broken, so keep the
     emphasis as colour/weight only (mirrors the .ital faux-italic guard). */
  html[lang='zh-CN'] .hero__sub em {
    font-style: normal;
    font-weight: 500;
  }
  .hero__sub strong {
    color: var(--m-ink);
    font-weight: 500;
  }
  .hero__reassure {
    margin-top: 14px;
    max-width: 46ch;
    font-size: 13.5px;
    line-height: 1.55;
    color: var(--m-ink-2);
  }
  .hero__points {
    margin-top: clamp(26px, 2.4vw, 34px);
    display: grid;
    gap: 9px;
    list-style: none;
    padding: 0;
  }
  .hero__points li {
    display: flex;
    align-items: flex-start;
    gap: 11px;
    font-size: 14px;
    color: var(--m-muted);
  }
  .hero__points svg {
    width: 18px;
    height: 18px;
    flex: none;
    margin-top: 1px;
    color: var(--m-accent);
  }
  .hero__points b {
    color: var(--m-ink);
    font-weight: 600;
  }

  .hero__cta {
    margin-top: clamp(30px, 3vw, 40px);
    display: flex;
    flex-wrap: wrap;
    gap: 10px 22px;
    align-items: center;
  }
  .btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 14.5px;
    font-weight: 500;
    border-radius: 8px;
    transition:
      background 0.18s ease,
      color 0.18s ease,
      box-shadow 0.18s ease,
      transform 0.12s ease;
  }
  .btn--primary {
    background: var(--m-accent);
    color: #fff;
    padding: 13px 22px;
    box-shadow: 0 1px 2px rgba(16, 24, 40, 0.08);
  }
  .btn--primary:hover {
    background: var(--m-accent-strong);
  }
  .btn--primary:active {
    transform: translateY(1px);
  }
  .btn--primary .arrow {
    transition: transform 0.18s ease;
  }
  .btn--primary:hover .arrow {
    transform: translateX(3px);
  }
  .btn--ghost {
    color: var(--m-ink);
    padding: 13px 4px;
  }
  .btn--ghost:hover {
    color: var(--m-accent);
    box-shadow: inset 0 -1.5px 0 var(--m-accent);
  }
  /* ---- alerts panel ---- */
  .panel {
    background: var(--m-surface);
    border: 1px solid var(--m-hairline);
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 24px 60px -44px rgba(16, 24, 40, 0.4);
  }
  .panel__head {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 13px 16px;
    border-bottom: 1px solid var(--m-hairline);
  }
  .panel__title {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    font-size: 12px;
    font-weight: 600;
    color: var(--m-ink-2);
  }
  .panel__dot {
    color: var(--m-faint);
  }
  .panel__live {
    width: 7px;
    height: 7px;
    border-radius: 999px;
    background: var(--m-ok);
    flex: none;
    position: relative;
  }
  .panel__live::after {
    content: '';
    position: absolute;
    inset: -4px;
    border-radius: 999px;
    border: 1px solid var(--m-ok);
    opacity: 0;
    animation: ripple 2.6s ease-out infinite;
  }
  @keyframes ripple {
    0% {
      opacity: 0.5;
      transform: scale(0.6);
    }
    70%,
    100% {
      opacity: 0;
      transform: scale(1.6);
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .panel__live::after {
      animation: none;
    }
  }
  .panel__count {
    font-size: 11px;
    color: var(--m-muted);
    font-family: var(--m-font-mono);
  }
  .panel__cap {
    margin-left: auto;
    font-size: 10px;
    letter-spacing: 0.04em;
    color: var(--m-faint);
    font-family: var(--m-font-mono);
    white-space: nowrap;
  }

  .panel__filter {
    display: flex;
    gap: 6px;
    padding: 10px 14px;
    border-bottom: 1px solid var(--m-hairline-2);
    overflow-x: auto;
    scrollbar-width: none;
  }
  .panel__filter::-webkit-scrollbar {
    display: none;
  }
  .chip {
    flex: none;
    font-size: 11px;
    font-weight: 500;
    color: var(--m-muted);
    padding: 4px 10px;
    border-radius: 999px;
    border: 1px solid var(--m-hairline);
    background: var(--m-surface);
    white-space: nowrap;
    cursor: pointer;
    transition:
      color 0.15s,
      border-color 0.15s,
      background 0.15s;
  }
  .chip:hover {
    color: var(--m-ink);
    border-color: var(--m-faint);
  }
  .chip--on {
    color: #fff;
    background: var(--m-accent);
    border-color: var(--m-accent);
  }

  .row {
    padding: 14px 16px;
  }
  .row + .row {
    border-top: 1px solid var(--m-hairline-2);
  }
  .row__top {
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .sev {
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.02em;
    padding: 2px 7px;
    border-radius: 6px;
  }
  .sev--urgent {
    color: var(--m-danger);
    background: color-mix(in srgb, var(--m-danger) 9%, transparent);
  }
  .sev--info {
    color: var(--m-ink-2);
    background: var(--m-section);
  }
  .juris {
    font-family: var(--m-font-mono);
    font-size: 11px;
    font-weight: 600;
    color: var(--m-ink-2);
    background: var(--m-section);
    border: 1px solid var(--m-hairline);
    border-radius: 6px;
    padding: 1px 7px;
  }
  .src {
    margin-left: auto;
    display: inline-flex;
    align-items: center;
    gap: 5px;
    font-family: var(--m-font-mono);
    font-size: 11px;
    color: var(--m-muted);
  }
  .src svg {
    width: 12px;
    height: 12px;
  }
  .row__title {
    margin-top: 9px;
    font-size: 14px;
    font-weight: 500;
    color: var(--m-ink);
    line-height: 1.35;
    letter-spacing: -0.01em;
  }
  /* Secondary rows recede so the anchor (urgent) row reads first. */
  .row--secondary .row__title {
    font-weight: 400;
    color: var(--m-ink-2);
  }
  .row__change {
    margin-top: 9px;
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
    font-family: var(--m-font-mono);
    font-size: 12px;
    color: var(--m-ink);
  }
  .row__change s {
    color: var(--m-muted);
  }
  .row__change .arrow {
    color: var(--m-muted);
  }
  .row__delta {
    font-family: var(--m-font-sans);
    font-size: 11px;
    font-weight: 600;
    padding: 1px 7px;
    border-radius: 999px;
  }
  .row__delta--later {
    color: var(--m-ok);
    background: color-mix(in srgb, var(--m-ok) 12%, transparent);
  }
  .row__badge {
    font-family: var(--m-font-sans);
    font-size: 11px;
    font-weight: 500;
    color: var(--m-muted);
    background: var(--m-section);
    border-radius: 999px;
    padding: 1px 8px;
  }
  .row__affect {
    margin-top: 11px;
    padding-top: 10px;
    border-top: 1px solid var(--m-hairline-2);
    display: flex;
    align-items: center;
    gap: 7px;
    font-size: 12px;
    color: var(--m-ink-2);
  }
  .row__affect svg {
    width: 14px;
    height: 14px;
    flex: none;
    color: var(--m-muted);
  }
  .row__affect b {
    color: var(--m-accent);
    font-weight: 600;
  }
  .row__status {
    margin-left: auto;
    font-size: 11px;
    color: var(--m-faint);
    font-family: var(--m-font-mono);
  }

  @media (max-width: 1000px) {
    .hero__inner {
      grid-template-columns: 1fr;
    }
    .hero__copy,
    .panel {
      grid-column: auto;
    }
  }
  @media (max-width: 540px) {
    .panel__head {
      flex-wrap: wrap;
    }
    .panel__cap {
      display: none;
    }
  }
</style>
