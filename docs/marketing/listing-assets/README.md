# Product screenshots — get-listed assets

Real product surfaces captured from the demo firm (account `plan-pro` via the e2e
demo-login — fictional data only, no real client PII). 2× resolution
(2880×1800 / element crops) for directory listings (G2, Capterra/GDM) and
community posts. Regenerate with `apps/app` running + server on :8787, using the
Playwright flow in the get-listed plan.

Every caption below states **shipped capability only** — safe to paste into a
listing as-is (see the honesty guardrails in
`../get-listed-directory-plan-2026-07-07.md`).

| File                      | Surface                 | Suggested caption                                                                                                                                                |
| ------------------------- | ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `01-today.png`            | `/today` dashboard      | "Your week at a glance: live source alerts up top, then the deadlines that need attention — overdue, awaiting documents, in review."                             |
| `02-alert-detail.png`     | Alert detail            | "Every change runs one path — Monitored → AI-parsed → Matched to clients → Your decision. The parsed rule, the affected client, and the source all in one view." |
| `03-affected-clients.png` | Affected-clients review | "When a rule changes, see exactly which clients it touches — entity type and location — ready to review."                                                        |
| `04-alert-source.png`     | Alert source / evidence | "A source on every date: the official IRS/state publication, the exact URL, the quoted text, and a captured-on date — verify before you act."                    |

## Notes for listings

- Lead with `02` (it tells the whole story) and `04` (the evidence proof) — these
  two carry the differentiation.
- Do **not** crop out the "AI parsed — verify before marking reviewed" language;
  the human-review gate is part of the honest pitch.
- The wordmark/avatar shows the demo user "Priya Pro" — fine to keep; it reads as
  a product demo, not a real customer.
