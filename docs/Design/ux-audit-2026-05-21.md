# UX audit — Dashboard, Obligations queue, Rule library, Client detail

**Date:** 2026-05-21
**Branch:** `design/preview-integration`
**Method:** Four independent design critiques (one per surface), reviewers
working from source code, no cross-coordination. Scoring on Nielsen's 10
heuristics (0-4 each, max 40). Personas: Sarah (owner, 8am morning glance),
Avery (preparer, junior), Miguel (manager, team load), Priya (partner,
governance), Jules (coordinator, no-dollars).

The Client detail surface was added in a follow-up pass (same date) after
the initial three-surface audit. It was scored against a 5th category —
**style consistency with the rest of the app** — using a parallel
mechanical pattern-adherence check.

This document is the source of truth for the prioritization that follows.
Re-run individual surface critiques after major changes to track score deltas.

## TL;DR

| Surface           | Score       | AI slop?   | Sentence                                                                                          |
| ----------------- | ----------- | ---------- | ------------------------------------------------------------------------------------------------- |
| Dashboard         | **24 / 40** | No         | Real product taste in action-rows; sabotaged by two versions shipping at once.                    |
| Obligations queue | **23 / 40** | Partial    | Strong individual primitives buried under an 8-column row with no primary anchor.                 |
| Rule library      | **26 / 40** | Borderline | Sharp review-mode workspace card; five horizontal bars of chrome before the matrix appears.       |
| Client detail     | **17 / 40** | Partial    | Hero is hand-crafted; body reads as a CRM accordion stack. 27% conformance to canonical patterns. |

The first three surfaces sit in the 23-26 band — moderate, with the same
underlying problems told three different ways. **Client detail sits
well below** that band because it rolls its own primitives instead of
consuming the shared ones (`<PageHeader>`, `<EmptyState>`,
`SectionFrame`/`SectionLabel`). **Fix the cross-cutting patterns first;
per-surface polish second.**

## Cross-cutting themes

### 1. Vocabulary fragmentation

The product cannot tell the same story twice the same way.

- **Pulse alerts** appear with three different names + icons + dismissal
  semantics across Dashboard banner, Inbox, and `/rules/pulse`.
  PulsingDot vs AlertTriangle vs hairline strip. "Pulse alert" vs
  "Need your decision" vs "Heartbeat strip." A new preparer cannot tell
  if these are the same thing.
- **Status taxonomy** is mid-migration. The route filter parses
  `pending | in_progress | waiting_on_client | review`; the actions list
  switches on `pending | in_progress | waiting_on_client | blocked | review
| completed`; the canonical 6-state target
  (`not_started/waiting_on_client/blocked/in_review/filed/completed`)
  matches neither.
- **Same color, different urgency.** RejectionChip and BlockedByChip both
  render in `text-text-destructive` on
  `bg-state-destructive-hover`. Rejection is "IRS sent it back"
  (actionable now). Blocked-by is "waiting on a dependency"
  (actionable but informational). Identical visual weight = noise.

### 2. Redundancy stack

Each surface has more chrome than content.

- **Dashboard:** v1 + v2 both ship in the route. `useDashboardV2`
  defaults to true on this branch but the v1 path (`DashboardTriagePanel`,
  `NeedsReviewBanner`, `LegacyPenaltyInline`, full filter facets) still
  mounts conditionally. `?dashboard=v1` URL resolves a different product.
- **Obligations queue:** 8 columns at peer visual weight (client, state,
  tax type, due, legacy penalty estimate, evidence, status, +chips). No primary
  anchor, no muted secondary, no progressive disclosure. The row is a
  spreadsheet, not a queue.
- **Rule library:** matrix view stacks 5 horizontal bars before the
  table: CoverageSummaryStrip + SourcesSummaryStrip + StatsStrip +
  SourceStatusBanner + EntityCoverageLegend. Active/pending counts appear
  twice on the same page (top summary strip + StatsStrip).

### 3. Invisible affordances

Power-user features exist but only power users know they exist.

- J/K/Esc hotkeys registered in obligations queue and rule review mode —
  zero in-UI hints. Priya enters review mode and has no way to discover
  she can press `j`.
- **Inline drawer close affordance:** modal mode gets Sheet's X + Esc +
  backdrop click. Inline mode has only a ghost "Close" button buried in
  the footer at `obligations.tsx:4430`. Three viewports, three different
  close contracts.
- **Rule library toggle:** `<Button variant="outline">View all rules →</Button>`
  floats at the strip's right edge. ChevronRight icon implies "navigate
  elsewhere," not "switch view." Users don't realize it's a persistent
  mode toggle until they click.

### 4. Missing safety net on destructive moves

- Status changes fire `updateStatusMutation` immediately. Success toast
  shows the audit hash but no `Undo` action. For destructive moves
  (`completed` → anything, `filed` → `in_review`) this is reversible in
  audit but invisible to the user.
- "Hide" on Dashboard System Status is session-only — no way to bring it
  back.
- No bulk-undo on bulk owner reassign.

## P0 burndown (do this week)

| #   | Issue                                                                                                                                                       | Surface       | Effort | Sequencing note                                              |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------ | ------------------------------------------------------------ |
| 1   | **Delete Dashboard v1.** `useDashboardV2` defaults to true; finish migration, remove the flag hook, delete v1 components.                                   | Dashboard     | M      | Frees the surface for further iteration.                     |
| 2   | **Unify Pulse vocabulary** across Dashboard banner / Inbox / `/rules/pulse`. One name, one icon, one severity scale, one dismissal semantic. Write it down. | Cross-cutting | M      | **Start here** — compounds across surfaces.                  |
| 3   | **Add Owner to Obligations queue row.** Avatar cell or "(You)" prefix on mine.                                                                              | Obligations   | S      | Unblocks Avery's 60-second triage.                           |
| 4   | **Real header X + Esc on inline drawer.** Match modal mode's contract across all three viewports.                                                           | Obligations   | S      | Closes the redesign's main hole.                             |
| 5   | **Kill Rule library redundancy stack.** Delete StatsStrip OR delete the two top strips. Active/pending must appear once.                                    | Rule library  | S      | Forces a real design call.                                   |
| 6   | **Differentiate RejectionChip from BlockedByChip.** Red stays for IRS rejection. Blocked-by → amber/info tone.                                              | Obligations   | XS     | One-line className change with high impact.                  |
| 7   | **Make hotkeys discoverable.** Visible `kbd` hints on review-mode panels and obligations queue.                                                             | Cross-cutting | S      | "j next · k prev · esc exit" right-aligned in panel headers. |

## P1 (next pass)

- Status change `Undo` action in success toast (Dashboard P1)
- Convert Rule library toggle into segmented control or tabs (Rule library P1)
- "What changed since" delta on Dashboard hero (Dashboard P1)
- Smart priority surfaced on Obligations row (Obligations P2)
- Filter chip visibility ≥1 filter, not ≥2 (Obligations P2)
- Redirect `/rules/coverage` → `/rules/library`; stop internal `navigate('/rules/coverage')` calls (Rule library P2)
- Status taxonomy convergence — land the canonical 6-state then audit every consumer (Dashboard P2)

## Persona red flags consolidated

**Sarah (owner, 8am morning glance):**

- Dashboard "Today" hero has no time-of-day delta. Every morning looks identical.
- Rule library "jurisdictions with gaps" count is buried under chrome.
- Obligations "at risk of missing deadline" requires 2-3 filter clicks; should be a single saved view.

**Avery (preparer, junior, week one):**

- Five proprietary terms above the Dashboard fold ("Legacy penalty estimate," "exposure,"
  "Smart Priority," "Needs input," "Pulse"). ConceptLabel covers one.
- No owner column on Obligations queue → can't separate "mine" from team load.
- Empty state CTAs contradict ("Run Migration Copilot" vs "Import clients").

**Miguel (manager, team load):**

- No per-owner load distribution view in Obligations queue. Lives in a
  separate `features/workload` directory.
- Bulk reassign exists but audit string leaks ("Obligations bulk owner change").

**Priya (partner, governance):**

- Hotkeys work but are invisible — first review session has no shortcut hints.
- Rule library "12 needs review" CTA appears AFTER she scrolls past two
  strips and the toggle button. Should be in the top strip.

**Jules (coordinator, no-dollars):**

- Permission-gated tiles silently disappear. v1 says "90-day projected
  risk hidden by role"; v2 says nothing. Coordinator sees a different
  shape of dashboard than partners and isn't told why.

## Provocative questions (don't answer yet)

1. **Is "Actions this week" actually a dashboard, or is it the Obligations
   queue with a 7-day filter and a sexier card?** If it's the same query,
   why does the user need both surfaces?

2. **Why does System Status sit above Pulse alerts on the Dashboard?**
   Source-health is an internal engineering concern; Pulse is client-facing.
   Reversing the order would put the loudest thing at the most-important
   thing.

3. **Eight columns + chip variations + status pill + due stack on the
   Obligations row: when does this stop being a queue and start being a
   spreadsheet?** Linear, Asana, Height each chose 4-5 columns and pushed
   the rest to row hover. What's the cost of saying no?

4. **If the two summary strips and the StatsStrip all show the same data,
   why do all three exist?** What does the Rule library lose if you delete
   two of them?

5. **The Dashboard has zero personalization by role.** A partner and a
   preparer see the same page. Should a preparer even see "Need your
   decision" (a review-tier action), or should that be partner-only?

## What's working (capture before changing it)

Per the critiques, these are real product insights that should NOT be
touched in the next pass:

- **Dashboard action-led rows** (verb-prompt over data) — tells juniors what to
  _do_, not what to _look at_. Rare and good.
- **Dashboard ExposureStrip tone discipline** — only `blocked` is destructive;
  everything else neutral. Dashboard refuses to be a fire alarm.
- **Obligations stacked Due cell** — relative pill + absolute date + statutory
  divergence in ~3 lines without a legend.
- **Obligations inline-vs-modal hybrid mechanics** — no focus trap in inline
  mode lets J/K swap panel content without close/open. Real product
  insight.
- **Rule library review-mode workspace card** — collapsing the page header,
  mounting a 320px queue rail + detail panel in one bordered surface is a
  sharp move. The feature's actual point of view.
- **`OriginBreadcrumb` pattern** with per-page Clear semantics — rare to see
  drill-ins respect return paths this cleanly.

## Client detail surface — follow-up pass

Added 2026-05-21 PM. Same method, plus a parallel **mechanical
pattern-adherence check** that compared the page primitive-by-primitive
to the canonical reference set (`PageHeader`, `EmptyState`,
`SectionFrame`/`SectionLabel`, Breadcrumb, Tabs, Button, Badge).

**Score: 17 / 40.** Lowest of the four surfaces.
**Conformance to canonical patterns: 27%** (6 conforms / 9 partial / 9 diverges out of 22 checks).

### Heuristic breakdown

| #   | Heuristic                      | Score | Why                                                                         |
| --- | ------------------------------ | ----- | --------------------------------------------------------------------------- |
| 1   | Visibility of System Status    | 2     | No "last refreshed", no live tab counts                                     |
| 2   | Match System & Real World      | 3     | CPA vocabulary lands well                                                   |
| 3   | User Control & Freedom         | **1** | No back button, no scroll-restore, can't dismiss alerts band                |
| 4   | Consistency & Standards        | **1** | Three parallel primitives (custom header / DetailSection / PanelEmptyState) |
| 5   | Error Prevention               | 2     | No inline confirms on saves                                                 |
| 6   | Recognition Rather Than Recall | 2     | Tabs hide structure; no count badges                                        |
| 7   | Flexibility & Efficiency       | **1** | No keyboard hints; no deep-link to a tab/section                            |
| 8   | Aesthetic & Minimalist         | 2     | Work tab = wall of accordions                                               |
| 9   | Error Recovery                 | 2     | Generic error alert; no retry                                               |
| 10  | Help & Documentation           | 1     | Almost nothing                                                              |

### Style divergences from canonical

| #   | What                                                                 | Where                                           | Should match                                                        |
| --- | -------------------------------------------------------------------- | ----------------------------------------------- | ------------------------------------------------------------------- |
| 1   | Custom card-framed hero instead of `<PageHeader>`                    | `ClientFactsWorkspace.tsx:1054-1074`            | `apps/app/src/components/patterns/page-header.tsx`                  |
| 2   | No action cluster — zero primary actions above the fold              | `ClientFactsWorkspace.tsx:1054`                 | `members-page.tsx:236`, `clients.tsx:295-307`                       |
| 3   | Custom `DetailSection` collapsible                                   | `ClientFactsWorkspace.tsx:170-222`              | `SectionFrame` + `SectionLabel` from `rules-console-primitives.tsx` |
| 4   | Custom `PanelEmptyState` + `ClientEmptyState`                        | `ClientFactsWorkspace.tsx:902-940`, `1613-1631` | `<EmptyState>` from `components/patterns/empty-state.tsx`           |
| 5   | Arbitrary type sizes: `text-[10px]`, `text-[11px]`, `tracking-wider` | 6+ sites                                        | Canonical `tracking-[0.08em]` + Badge primitive sizes               |
| 6   | Pseudo-links: bare `<a>` / `<button>` with `text-text-accent`        | `:1486`, `:1543`                                | `Button variant="ghost" size="sm"`                                  |
| 7   | Page-level gap is `gap-5` not `gap-6`                                | route + workspace                               | Dashboard / Obligations / Audit / Members all use `gap-6`           |
| 8   | "Not found" Alert has no recovery action                             | `clients.$clientId.tsx:57-66`                   | Should offer `<Link to="/clients">Back to clients</Link>`           |
| 9   | Title uses `<h2>` inside a bordered card                             | `ClientFactsWorkspace.tsx:1054-1061`            | Canonical is flat `<h1>`                                            |

### Persona red flags

- **Avery (preparer):** can't one-click into the next due obligation from the hero. Has to scroll → open Filing plan → find the row. The 5-second-triage test fails.
- **Sarah (owner):** Pulse risk signal is visible but "is this client at risk?" lives in the Notes tab behind a default-open AI summary.
- **Miguel (manager):** **owner / assignee is not in the hero at all.** No view of "who's on this client" without opening the Risk inputs panel or the queue.

### What's working (preserve)

- `ClientAlertsBand` — single warning-toned strip that consolidates Pulse alerts + extension mismatches + missing facts. App-native, strong pattern.
- Filing-plan year-grouping (`:1296-1351`) — editorial, CPA-specific, not generic CRM.
- Badge variants use semantic tokens correctly.
- `useObligationDrawer()` is wired (post the obligation-drawer-as-shared-overlay refactor).
- Tabs use the canonical `<Tabs>` primitive.

### Provocative questions

1. If Mailbox is "Phase 2", why is it occupying a top-level tab slot today instead of a stub state inside Work?
2. Why does the hero show five badges (entity-type + state + readiness + source + Pulse) before the client name is parsed?
3. Should the default landing scroll target be the next-due obligation row, not the top of the page?

### Sequencing decision

User chose: **IA first, then style alignment.** Pass 1 swaps the hero to `<PageHeader>` + adds a 3-tile summary strip (Next due / At risk / Team) above the tabs + surfaces primary owner in the identity strip + removes the Mailbox tab. Pass 2 lifts `DetailSection` and the custom empty states onto the canonical primitives, normalizes type tokens, retires pseudo-links, and adds 404 recovery.

## What this audit doesn't cover

- **Mobile.** None of the three critiques opened DevTools below md
  breakpoint. The breadcrumb at 11px on a 375px viewport, drawer panels
  that shift between inline/modal/full-screen — needs its own pass.
- **Empty / loading / error states.** Spot-checked, not exhaustive.
- **Tab title + sidebar active state alignment** when in compact mode
  (rule review). Four wayfinding signals, only one tells the truth.
- **Notification channel consistency beyond Pulse.** Reminders, audit
  events, system status, billing alerts all surface somewhere — the same
  fragmentation likely exists.
- **Onboarding / first-run.** Empty product is invisible in this audit.

## Sequencing decision

**Pulse vocabulary unification first** (P0 #2). Reason: cross-cutting,
unblocks future per-surface work, and the inconsistency compounds every
time we add a new mount.

Tracked in `apps/app/docs/dev-log/` once each landing PR ships.
