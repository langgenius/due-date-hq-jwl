# Client detail — "Filings" tab label + count pill (VtC73)

**Date:** 2026-06-10

Pencil `VtC73` labels the first tab **"Filings"** with a count pill, not "Filing
plan". Renamed the trigger and added a token-styled count pill (`bg-background-
section` rounded-full, `obligations.length`), shown when the rail is collapsed.
The body heading + table layout are a separate (larger) piece. tsgo clean.
