# Inspiration flexible-map — critical decision ledger

_2026-06-21 · per-opportunity adjudication of all 124 high-value opportunities against the real code._

Every high-value opportunity from the flexible-opportunity map was re-verified
individually against the actual codebase by an adjudication agent that READ the
source (the earlier "net-new" novelty tags were discarded as unreliable — they
mislabelled fully-built features as net-new). Each got one grounded verdict.

## Verdict distribution

| Verdict           | Count   |
| ----------------- | ------- |
| `buildable`       | 31      |
| `already-built`   | 37      |
| `fiction-blocked` | 26      |
| `canon-reject`    | 21      |
| `duplicate`       | 4       |
| **total**         | **119** |

Headline: **88 of 124 were NOT actionable** (already-built / fiction / canon-reject /
duplicate). **0 reached P1** — no high-value-low-risk headline feature remains. The 31
buildable items are all incremental polish (15 P2, 16 P3).

## Buildable (31)

### P2 (15)

- **img-017** · SeverityChip / AlertsListPage — /alerts — alert feed when multiple URGENT/HIGH alerts are new and unreviewed
  - spec: In AlertsListPage, compute urgentUnreviewedCount from the alerts query; when ≥3 and the Review tab is not already active, render an InfoBanner above the list rail: 'N unreviewed urgent changes are in effect' + a 'Review urgent' Button that applies a priority=urgent filter.
  - evidence: No urgent-count contextual banner exists above the alerts list. PulsePriorityLevelSchema ('normal'|'high'|'urgent') exists in packages/contracts/src/pulse.ts line 76, so the severity data is real. AlertsListPage already tracks workQueueCounts (Review tab). A banner surfacing 'N unreviewed urgent changes are in effect' with a 'Review all' CTA (opening MorningSweepDialog or filtering to urgent) is backed by real data, honors demote-don't-delete (not a modal), and the InfoBanner primitive exists. Threshold ≥3 urgent unreviewed needed.
- **img-022** · Priorities table — /today — Priorities section
  - spec: In MergedBriefCard's TableBody, insert a sticky CapsFieldLabel section-divider row between Today / This week / Later buckets derived from each row's daysUntilDue against asOfDate; no new API required.
  - evidence: MergedBriefCard (apps/app/src/features/dashboard/merged-brief-card.tsx) uses bucket chips (This week / This month / Overdue) as a top-level table filter — one bucket shown at a time. The proposal adds 'Today / This week / Later' as in-row section headers separating the rows within the displayed bucket. No such grouping rows exist in the current table (confirmed by grep for 'isToday', 'isThisWeek', 'isLater', 'section header' returning no matches in the file). dueDate is real data on every DashboardTopRow. This is structural grouping (not count lines), so it does not conflict with the 'workload-counts deliberately cut' canon.
- **img-026** · AlertDetailDrawer — affected-clients table — /alerts detail
  - spec: In AffectedClientsTable, group rows by PulseAffectedClient.status with a collapsible section header per group (not_started / waiting_on_client / in_review), each with a row count; preserve existing matchStatus sorting within each group.
  - evidence: PulseAffectedClient carries a real 'status: ObligationStatusSchema' field (packages/contracts/src/pulse.ts line 206). AffectedClientsTable (apps/app/src/features/alerts/components/AffectedClientsTable.tsx) renders a flat list ordered only by needs_review priority — no grouping by obligation status. The grouping (not_started / waiting_on_client / in_review) is entirely absent. Canon-OK.
- **img-051** · priorities table — /today priorities section
  - spec: In the collapsed DailyBriefCard tab (daily-brief-card.tsx), render a deterministic count summary from `todayCounts` as the teaser when `aiEnabled && !brief?.text`, so CPAs see 'You have N overdue, N waiting on client' immediately without waiting for the AI brief.
  - evidence: Two proposals: (1) Deterministic count teaser in collapsed tab — daily-brief-card.tsx line 148-154 derives the teaser from AI headline text (`parseBriefText(brief.text).headline`). When brief is null or AI is disabled, `teaser` is null and no count-based fallback renders. `DailyBriefTodayCounts` (overdueCount, waitingOnClientCount, dueThisWeekCount) is already passed as a prop and available synchronously. A 'You have 3 overdue, 1 waiting on client' deterministic fallback is genuinely absent. (2) Asymmetric two-button topmost overdue row — ActionsTableRow at actions-list.tsx line 249+ shows a single hover 'Review' button for all rows. The first overdue row with a second 'Mark waiting on client' action button is not implemented. Both sub-proposals verified as missing. Rendering the first as the primary buildable.
- **img-054** · StatusRing / StatusMark — /deadlines table, obligation detail drawer, /today priorities table
  - spec: Add a `StatusPill` export to status-control.tsx that renders `<StatusRing>` + the canonical v2 status label inside a low-opacity tinted pill (bg at 10% of the status color, text + icon at full status tone) — for use in the obligation detail hero and the workflow strip active-stage header.
  - evidence: StatusRing in apps/app/src/components/primitives/status-ring.tsx is icon-only (SVG glyph). StatusMark in status-control.tsx (line 130+) wraps StatusRing with color class. No `StatusPill` mode (icon + label text in a tinted pill container) exists anywhere in the codebase — grep for 'StatusPill' returns only unrelated AuthStatusPill and member pills. The 6 canonical status states and their color tokens are real and already used via STATUS_ICON_COLOR.
- **img-070** · onboarding setup checklist / banner (Proposal B) — /today or /splash — persistent setup banner
  - spec: Add an inline setup-progress tick bar to /today (below page header, above NeedsAttentionSection): one tick per setup step (source connected, client added, first rule active), brand-accent fill for done ticks, muted gray for pending, percent badge right; self-dismisses when all ticks fill. No server-side flags needed — derive from sourceCount > 0, clientCount > 0, activeRuleTotal > 0.
  - evidence: No persistent setup banner or segmented tick bar exists anywhere in the codebase. The dashboard shows two first-run empty states (showFirstRun / needsRules) but no persistent progress indicator visible after those resolve. The contracts carry queryable signals: client count (obligations.ts line 602), source count (rules.ts line 445), and rule activeRuleCount — all real. The onboarding task (#29 'Onboarding B: persistent setup checklist/banner') is listed as completed in task history but the component does not exist in the source tree. The segmented tick bar pattern is new.
- **img-073** · AffectedClientsTable — /alerts alert detail — affected-clients module
  - spec: Above the AffectedClientsTable, render up to 5 AssigneeAvatars in a -space-x-1.5 overlap cluster using real client names from affectedClients rows, followed by a CountPill showing +N overflow when rows > 5 — replacing the bare card-title count.
  - evidence: AffectedClientsTable.tsx renders a full bordered table starting immediately; there is no stacked-avatar cluster or CountPill summary above it in the drawer. The section header is just the card title 'Clients'. AssigneeAvatar exists (apps/app/src/features/obligations/AssigneeAvatar.tsx) and CountPill exists as a primitive. PulseAffectedClient rows carry clientName (used in AffectedClientsTable). No AvatarGroup/stacked-avatar component exists — it would need to be built inline. The data is real and the primitives are canon-OK.
- **img-085** · BlockerContextCard — /deadlines detail — Status tab, blocked stage
  - spec: In BlockerContextCard, after the existing status/due row, append the 1-2 most recent audit events with action=obligation.status.updated/blocked using the transition-row anatomy (arrow + mono enum + source chip + timestamp) — data from the auditEvents array already fetched by the parent Status tab.
  - evidence: BlockerContextCard.tsx receives blockerId and fetches orpc.obligations.getDetail. The parent panels.tsx already fetches auditEvents (used for PathToFilingSummary, auto-unblock detection at line 2192). Audit events with trigger phrases are real backend data and the transition-row pattern is established. No new data fetches required.
- **img-086** · workflow stage strip — /deadlines detail — Status tab
  - spec: Reorient PathToFilingSummary (panels.tsx) from horizontal grid-cols-6 to a vertical flex-col list; add a micro-subtitle line per stage showing the real trigger phrase (from auditEvents mined by mineTimelineTimestamps) and a timestamp, with active stage at ~40% visual weight per the asymmetric-stage-attention canon.
  - evidence: PathToFilingSummary (panels.tsx line 784) is currently a horizontal 'grid grid-cols-6'. It already consumes auditEvents and calls mineTimelineTimestamps to extract per-stage timestamps. The 6 lifecycle stages and transition log with trigger phrases are real. The detail pane (not the list rail) is the correct surface per the asymmetric-attention canon.
- **img-113** · ListRail (alerts list) — /alerts list rail
  - spec: Add `SeverityChip` import to `AlertListRail.tsx` and render it alongside the existing change-kind label in the badge row (line 337), matching the level mapping already used in `PulseAlertRow`.
  - evidence: `AlertListRail.tsx` imports `ChangeKindIcon`/`changeKindLabel` and renders them (lines 338–339) but does NOT import or render `SeverityChip`. The main list rows (`PulseAlertRow.tsx` line 500) do render `SeverityChip` with real `alert.severity` data. The chip primitive exists in `@/components/primitives/severity-chip` and the severity field is real on the alert contract. The dual-chip gap (severity missing from rail, change-kind present) is confirmed by reading both files.
- **img-125** · FilterTrigger + popover (change-kind filter on /alerts) — /alerts
  - spec: Upgrade AlertChangeKindFilter and AlertTaxAreaFilter dropdowns from single-select DropdownMenuRadioGroup to multi-select checkbox popovers with Select-all toggle, per-option Checkbox rows, and count valueLabel in the FilterTrigger pill.
  - evidence: AlertsListPage.tsx line 34 confirms AlertChangeKindFilter and AlertTaxAreaFilter currently use DropdownMenuRadioGroup (single-select, line 1053-1074). The type definitions show these are typed as single values ('all' | specific-enum). Multi-select checkbox pattern with 'Select all' + indeterminate + count label in pill ('Type | 3 ⌄') does not exist. Real change-kind and tax-area data comes from the live alerts list. This is canon-OK (FilterTrigger Label│Value pattern is documented).
- **img-134** · DetailSectionCard (obligation Status tab metadata: assignee, due date, form type, jurisdiction) — /deadlines/[id] Status tab
  - spec: In the Status tab metadata DetailSectionCard, replace CapsFieldLabel-stacked pairs for form/jurisdiction/assignee/dueDate/createdAt with horizontal icon-left + 120px fixed-width label + right-aligned value rows separated by hairline dividers.
  - evidence: The obligation Status tab metadata (form, jurisdiction, assignee, due date, created date) currently uses CapsFieldLabel stacked above values (apps/app/src/features/obligations/queue/ObligationQueueDetailDrawer.tsx uses CapsFieldLabel in many places). No horizontal icon-left + 120px-fixed-label + right-aligned-value row pattern exists for this Status-tab obligation metadata card. All fields (formName, jurisdiction, assigneeId, dueDate, createdAt) are real on ObligationInstancePublic (contracts/src/obligation-instance.ts line 60). This recovers vertical space and matches the 'horizontal key→value' reference pattern used in alert detail.
- **img-136** · Rules accept/review modal family (single-accept modal, bulk review) — /rules/library rule review modal — oei7Q / Fzzoq
  - spec: In splitRail mode of RuleDetailDrawer, add a read-only 'After' obligation card preview in the right rail below the ValueDiff, derived from the rule draft's formName/jurisdiction/newDueDate fields — no new data fetch required.
  - evidence: The rule accept modal (apps/app/src/features/rules/rule-detail-drawer.tsx) has a splitRail layout (line 629) with left='Verify the facts' and right='Your decision' rail. The right rail contains impactCard/beforeAcceptCard/practiceCard/CandidateReviewSection but no 'After' preview of the obligation card as it will look post-apply. formName, jurisdiction, newDueDate, currentDueDate are already in the rule draft payload (contracts/src/rules.ts). ValueDiff is already a built primitive. The after-preview is derivable from the same mutation input with zero new fetches.
- **img-160** · DetailSectionCard (obligation metadata section) — /deadlines/:id Status tab metadata region
  - spec: Status tab metadata region: render formName, recurrence (Annual/Quarterly/…), jurisdiction, and taxPeriod window as icon+value chips in a flex-wrap row using existing chip styles from the header row (~line 2207); no new schema needed.
  - evidence: ObligationInstancePublicSchema (packages/contracts/src/obligation-instance.ts) has formName, recurrence, jurisdiction, taxPeriodStart/End, taxYear. The current header chip row in ObligationQueueDetailDrawer.tsx (~line 2207) only renders the client chip + optional JurisdictionLabel — no 2xN metadata chip grid showing all key facts. Metadata grid is genuinely absent.
- **img-195** · DetailSectionCard (onboarding / settings member section) — /settings — firm member management
  - spec: Replace InviteMemberDialog modal with a sticky inline invite row (email Input + role Select + Send button in one flex row) above the scrollable member list in apps/app/src/features/members/members-page.tsx, reusing existing orpc.members.invite mutation and MemberManagedRole enum.
  - evidence: Current UX: invite lives behind a Dialog (InviteMemberDialog, line 1114) opened by a header button. Member list + invite mutation (orpc.members.invite) + role enum (MemberManagedRole) are all real. No existing inline-invite row pattern in the members page. FilterTrigger primitive exists for the role slot. No fiction dependencies.

### P3 (16)

- **img-015** · JurisdictionChip / TaxCodeBadge — /deadlines table — obligation rows, /clients client-detail tabs
  - spec: Add a jurisdiction-tier prop (federal / state / local / other) to JurisdictionChip that maps to 4 fixed design-token tonal variants; derive tier from code (FED/IRS → federal, 2-letter → state, city codes → local); apply same mapping to TaxCodeBadge's background.
  - evidence: JurisdictionChip uses a uniform outline Badge (no tonal palette, apps/app/src/components/primitives/state-badge.tsx lines 371-386). TaxCodeBadge uses monochrome. No hue-family grouping by federal/state/local exists anywhere. Real data: obligation.jurisdiction and obligation.taxType are on every obligation row. The 4-5 fixed hue families mapped to existing design token tones (not raw colors) is feasible and canon-safe (design-token tones, not red+bold, not per-value random). P3: visual grouping is nice-to-have, not triage-critical.
- **img-018** · DueCountdownText / DueDaysPill — /deadlines table — due date column, /today priorities table
  - spec: In DueDaysPill, when days < 0 (overdue), promote the span to text-base instead of text-sm; keep DueCountdownText + its tone class unchanged.
  - evidence: DueDaysPill in apps/app/src/features/obligations/queue/components/primitives.tsx lines 82-87 always renders text-sm regardless of whether days < 0 (overdue). No text-base scale promotion exists. daysUntilDue is real data on every obligation row. Pattern is size-only (no bold), which satisfies the type-weight-restraint canon.
- **img-019** · StatBand — /clients — client detail workspace, StatBand on client detail surface
  - spec: Add a Focus mode ToggleChip to the /deadlines filter bar that, when active, reduces the table to one row per client (lowest daysUntilDue, highest severity) and shows a CountPill with the hidden count.
  - evidence: No Focus mode ToggleChip exists in the /deadlines filter bar (searched obligations.tsx filter bar ~line 6120 area — no focusMode state, no per-client grouping toggle). obligation.dueDate, obligation.risk, and client grouping are all real fields in the obligations query. Canon-OK: ToggleChip is the canonical primitive for this affordance.
- **img-024** · EmptyState — /deadlines — when filtered to current period and all obligations are completed
  - spec: Add an 'all-completed' EmptyState variant to ObligationQueueEmptyState triggered when hasActiveFilters=false and allObligationsCompleted=true; use a CSS-transform single-keyframe calendar-page flip animation with copy 'All deadlines filed for this period.'
  - evidence: ObligationQueueEmptyState (apps/app/src/features/obligations/queue/components/toolbar.tsx lines 274-325) handles 'no results with filters' and 'genuinely empty workspace' but has no all-completed celebration state. The filtered obligation list already provides enough data to derive allObligationsCompleted (every row.status === 'completed'). No existing EmptyState variant covers this trigger. Canon-OK: delight is specifically sanctioned for earned completion moments.
- **img-036** · ValueDiff (before→after) — /alerts detail — Change section, /rules detail
  - spec: Add a 'wire' mode to ValueDiff that renders old value and new value as two compact node-cards (radius-8, border, no shadow) connected by an ArrowRightIcon line; deleted token on the source node, added token on the destination node.
  - evidence: ValueDiff exists at apps/app/src/components/primitives/value-diff.tsx with 'inline' and 'compact' modes only. No 'wire' or 'node-card' mode exists. The PulseAlert change payload carries oldValue/newValue (used by existing ValueDiff consumers in PulseAlertRow.tsx line 694). New rendering mode uses the same real data. Canon-OK: uses DetailSectionCard tokens (radius-8, border, no shadow — all within the radius scale).
- **img-041** · EmptyState (ghost-card) — /rules library, /sources — first-run zero state
  - spec: On /rules/library and /rules/sources zero state, replace the plain EmptyState with a 3-cell ghost-card grid of skeleton bars at correct column widths (name, jurisdiction chip, status badge) plus a 1-line instruction beneath each cell, before the existing CTA.
  - evidence: \_RulesLibraryEmptyState (apps/app/src/routes/rules.library.tsx lines 3185-3213) shows a canonical EmptyState with icon + text + CTA — no skeleton/ghost rows showing data shape. The rules library and sources surfaces have no 'thumbnail-as-proof' ghost card. The rules table column structure (name bar, jurisdiction chip, status badge widths) is real and stable. Canon-OK: ghost cards are within the EmptyState pattern family (already listed as BUILT for ghost-card EmptyState, but that is the base primitive; this is a specific application variant not yet instantiated on these surfaces).
- **img-047** · workflow stage strip — /deadlines detail drawer — Status tab
  - spec: Add a `compact` responsive mode to ActionsTableRow at ≤md that collapses to obligation name (500) + StatusRing right-aligned + DueCountdownText as muted sub-label, driven by existing DashboardTopRow data.
  - evidence: Two proposals under img-047: (1) Compact mobile collapse of the priorities table — apps/app/src/features/dashboard/actions-list.tsx ActionsTableRow renders a fixed 5-column table with no mobile compact mode; the today-responsive contract specifies overflow-x-auto for this table, not a single-row collapse. Real data (status, name, dueDate from DashboardTopRow) is available. Canon-OK. (2) Blocking checklist badge rows in the active stage card — ActiveStageDetailCard in panels.tsx (line 1271+) shows a count summary and sub-status text, not per-item name+badge rows; those rows live only in the Materials tab (ChecklistItemRow.tsx). However this second proposal duplicates Materials tab content, violating 'one purpose per panel'. Rendering the first sub-proposal only.
- **img-053** · audit transition row — /audit-log, obligation Audit tab, /today portfolio stream
  - spec: In AuditTimelineRow (audit-log-table.tsx), parse rule ids from `event.reason` and render them as small gray outlined Badge chips (variant='outline' size='sm') inline in the metadata strip, so the row reads as a structured sentence rather than undifferentiated mono prose.
  - evidence: In audit-log-table.tsx AuditTimelineRow (line ~311-317), metaChips render as plain mono text: `<span className="font-mono text-caption-xs text-text-tertiary">`. The changeHeadline from `buildAuditChangeView` is a flat string. No outlined pill chips exist in the audit row to tokenize rule names or trigger keywords. AuditEventPublic has `action`, `beforeJson`, `afterJson`, `reason` — all real. An inline token-chip treatment parsing the rule id from the changeHeadline or reason field is genuinely absent.
- **img-067-apply-gate** · apply gate / Your Decision section in AlertDetailDrawer — /alerts detail — apply gate
  - spec: Replace the low-confidence verbose Alert and the silent not-ready gate with a single-line bg-background-subtle bar: diagnostic sentence left (derived from applyReadiness.missing[]), 'Review source' TextLink right — rendered above the Apply button only when applyReadiness.status !== 'ready'.
  - evidence: The current not-ready apply gate (applyReadiness.status !== 'ready') renders nothing in the pending band (line 657: 'return null') and shows a verbose multi-line Alert component for low-confidence (lines 1910-1937). There is no single-line gray diagnostic bar above the Apply button. The contract carries applyReadiness.status ('needs_details' or 'not_applicable') and applyReadiness.missing[] — enough to derive a one-line message. The apply button is real. This is genuinely absent and canon-OK (restrained, no double-signal).
- **img-077-obligation-sentence** · daily-brief-card — /today dashboard — DailyBriefCard AI summary
  - spec: In the Status section summary area, render a prose sentence using existing primitives: '<JurisdictionChip> [formCode] return for [clientName] · due <DueCountdownText> · every [frequency]' — replacing the stacked label-value grid in the obligation overview.
  - evidence: The obligation drawer header currently renders form code as h2 and client name as a kicker (ObligationQueueDetailDrawer.tsx line 1651-1654), with jurisdiction/tax-year/period as separate secondary meta lines (line 2050). There is no natural-language sentence pattern anywhere in the obligation summary section. All the data fields are real (jurisdiction, formCode, clientName, currentDueDate, frequency). The JurisdictionChip, TaxCodeBadge, DueCountdownText, and ToggleChip primitives are confirmed built. This is a genuinely absent display pattern — it would be additive in the Status tab summary section, not a replacement of any existing single-source element.
- **img-091** · StatusBanner (auto-unblock banner) — /deadlines detail — Status tab, blocked/pending state
  - spec: In the pending-stage auto-unblock card (panels.tsx line 2212), replace bg-background-subtle with a CSS repeating-linear-gradient(45deg, transparent, transparent 6px, amber/8% 6px, amber/8% 8px) background to signal 'caution, action needed', keeping the existing eyebrow + prose content.
  - evidence: Auto-unblock banner is real (panels.tsx lines 2190-2225, obligation.status.auto_unblocked event from real audit log). It is currently a plain bg-background-subtle rounded card. Diagonal CSS hatching is self-contained with no new data. Canon allows CSS-only cosmetic changes; no per-side border on rounded corners is avoided (hatching is a background, not a border-side).
- **img-093** · EmptyState (ghost-card) — /sources — empty state (no sources added)
  - spec: When sources list is empty (rows.length === 0), replace the current compact EmptyState with two stacked feature-discovery cards: card 1 'Connect a source' + MonitoringChip schematic + 'Learn more' TextLink; card 2 'AI reads changes for you' + ValueDiff schematic (before→after date) + TextLink — both backed by real MonitoringChip and ValueDiff components.
  - evidence: SourcesTab (sources-tab.tsx line 321) renders EmptyState when rows.length === 0 with placeholder description. MonitoringChip and ValueDiff are confirmed real components in the codebase (already-built list). The two-card pattern requires no new data, only a layout redesign of the zero-state path.
- **img-123** · FloatingActionBar — /deadlines (obligations table bulk selection)
  - spec: Replace the bulk Set-status DropdownMenuRadioGroup with a popover that renders each of 6 ObligationStatus options as an icon (StatusMark) + label row, matching the canonical STATUS_VARIANT tones.
  - evidence: FloatingActionBar bulk 'Set status' uses a DropdownMenuRadioGroup (apps/app/src/routes/obligations.tsx line 1529-1620). There is no flyout panel with StatusRing icons alongside each of the 6 states. The StatusMark component is available and the 6 canonical statuses are real. This is a pure UI enhancement over the existing dropdown — no new data required.
- **img-149** · priorities table — /today
  - spec: In the actions-list row hover-expand panel, below the Review CTA, render 2-3 icon+value attribute lines (CalendarIcon + DueCountdownText, ObligationStatusReadBadge, JurisdictionChip) using real row fields.
  - evidence: actions-list.tsx has a hover state (group CSS, gradient mask, Review button) but the expanded reveal shows only the 'Review' CTA button — no DueCountdownText+StatusRing+JurisdictionChip attribute lines. The row spec in the comment (line 83–93) lists DUE and STATUS as separate columns but the hover-expand shows no inline triage context. Due date, status, and jurisdiction are all real fields on every dashboard row (DashboardTopRow type). This is genuinely missing. Canon-OK: icon+value attribute lines use no forbidden patterns. Primitives DueCountdownText, StatusRing (ObligationStatusReadBadge), JurisdictionChip all exist.
- **img-153-due-countdown** · DueCountdownText / Tooltip — /deadlines table and /today priorities table
  - spec: Add a Tooltip to DueDateLabel that shows 'Due date · [formatted actual date]' and, when terminal, 'Filed · [filed date]' as structured rows, using the existing Tooltip primitive.
  - evidence: DueCountdownText / DueDateLabel (due-date-label.tsx) has no tooltip — it renders plain text with no hover state. The actual due date and filed date are real fields on every obligation row. No Tooltip wraps DueDateLabel at any call site in actions-list.tsx or obligations.tsx. The proposal (show 'Due date · [actual date]' + 'Filed · [filed date]' structured pill rows on hover) is genuinely absent. Canon-OK: tooltips on compact chips are the established pattern (TaxCodeBadge, ValueDiff compact). Real data available (currentDueDate, filed date on ObligationStatus).
- **img-175** · workflow stage strip — /deadlines detail
  - spec: On the active stage node of PathToFilingSummary, render a compact text fraction chip (receivedCount/checklistCount, hidden when checklistCount===0) beneath the stage label, sourced from the already-loaded readiness data passed down from the drawer.
  - evidence: ReadinessOverview (panels.tsx line 70) receives checklistCount + receivedCount and is currently rendered only inside the Materials tab body (ObligationQueueDetailDrawer.tsx ~line 3311). The PathToFilingSummary horizontal strip (panels.tsx line 799) has no checklist fraction indicator. The data (checklist.length + received count) is already loaded in the drawer context. Adding a compact '2/5 items' chip on the active stage node of the strip is genuinely absent.

## Already built (37)

- **img-003** · alert cards (change feed list) — The /alerts queue has a Review tab (vs Active tab) in AlertsListPage that already segregates undecided alerts from active ones. The tab shows a warning-tone count when non-zero (AlertsListPage.tsx lines 843-868). A separate floating zone marker with collapsing behavior would duplicate the Review tab's function. The 'decision=null' state is already the definition of the Review queue tab — the separation IS the zone.
- **img-005** · workflow stage strip (apply/propagate stage) — ApplyingPill already exists in apps/app/src/features/alerts/components/ApplyingPill.tsx — a gradient-sweep conic-gradient ring animation during apply mutation in-flight, shown via {isMutating ? <ApplyingPill /> : null} in AlertDetailDrawer.tsx line 2678. The paper-plane-translating-rightward variant is a different micro-interaction, but it would replace the existing ApplyingPill with a redundant treatment on the same signal. The apply in-flight state is already addressed.
- **img-006** · rules review modal family (accept/reject) — The reviewer identity is already surfaced post-accept in the rule detail drawer's VerificationSection (apps/app/src/features/rules/rule-detail-drawer.tsx lines 2469-2491) as 'Reviewed by [name] / Reviewed at [timestamp]', backed by reviewedBy (text foreign key to user.id) in packages/db/src/schema/rules.ts lines 105, 135, 172. The MODAL confirm moment is not the right home per the 'one purpose per panel' canon — the confirmation action's purpose is committing, not displaying identity; identity already has a home in the drawer's Practice review section.
- **img-008** · EmptyState (ghost-card visual) — MergedBriefCard (apps/app/src/features/dashboard/merged-brief-card.tsx lines 335-358) already has the all-clear empty state: CoffeeIcon in an accent disc with 'All clear — nothing due, nothing late.' and a sub-line, rendered when totalActive === 0. This is the 'earned rest' delight moment the opportunity describes. The specific illustration (face-down burnout) is different from CoffeeIcon, but the design decision is made and the all-clear state is built.
- **img-011** · EmptyState (ghost-card visual) — EmptyState component (apps/app/src/components/patterns/empty-state.tsx lines 59-109) already has visual='ghost-cards' — the fanned placeholder deck (img-055 cited in the code comment) showing blank skeleton cards angled behind a front card. This IS the ghost-card visual for a backlog state. The >20-alerts threshold trigger is a minor condition on an already-built visual. The 'Review all' bulk action pattern also already exists via MorningSweepHeaderButton in alerts.tsx.
- **img-048** · daily-brief card — DailyBriefCard already handles the allQuiet state: `FirmTodayLine` at daily-brief-card.tsx line 493-500 checks `counts.overdueCount === 0 && counts.waitingOnClientCount === 0 && counts.dueThisWeekCount === 0` and renders 'No deadline pressure right now.' At personal scope with a failed brief and no catchup rows, the collapsed tab shows 'All quiet — nothing new needs your attention right now.' (line 210). The quiet state is already handled with deterministic counts; the hero-mode icon + 'All clear for today' + CTA variant is a cosmetic variant of an already-addressed state.
- **img-057** · DetailSectionCard — The rule detail hero (RuleDetailHeroCard in apps/app/src/routes/rules.library.tsx line ~4457-4519) already renders: status badge ('Awaiting review'/'Active'), queue age + review reason text, and AI confidence as 'AI N%' with SparklesIcon (line 4500-4509). The confidence field comes from `concreteDraft?.draft?.confidence` on the RuleConcreteDraftSchema. The proposed 'category badge' (Extension/Penalty/Date change) is a distinct concern, but ObligationRule schema has no category field — only `eventType`, `obligationType`, `overrideType` (on TemporaryRule). Since the confidence and title+badge header is already implemented, and the specific 'category' badge has no backing field, the built portion dominates.
- **img-060** · StatusRing — ClientSummaryStrip.tsx line 224-238 already renders a linear progress bar at the strip's bottom showing `filedCount/totalObligations` as a filled green bar with the fraction label '12/32'. This is precisely the data and semantic purpose of the proposed StatusRing corner decoration — filed-of-total progress on the client. Adding a 16px StatusRing beside the Filed cell would be a duplicate visual encoding of the same fact already expressed by the progress bar 2px below.
- **img-061** · audit transition row / Activity scroll-spy section in AlertDetailDrawer — AlertActivityTimeline function in apps/app/src/features/alerts/AlertDetailDrawer.tsx lines 387-551 renders exactly this pattern: an <ol> of steps with CircleCheckIcon (done, green), accent-ring target (current, blue), hollow ring (future), connected by a w-px flex-1 bg-divider-subtle rail, with step title + meta on the left and a mono timestamp on the right. Most-recent step is full-opacity; future steps use text-text-muted. Mounted at line 2327 inside the Activity DetailSectionCard.
- **img-067-workflow-strip-banner** · ? — The blocking/waiting context is already surfaced INSIDE the active-stage card, not between strip and card. panels.tsx line 2176 renders BlockerContextCard inline when stageKey==='blocked'; lines 2142-2162 render an overdue banner; lines 1504-1565 wire waiting_on_client tasks including Send-reminder. The BlockerContextCard IS the 'short sentence + action' pattern — just positioned within the card body, not above it. The concept is built; the pixel location is the only delta and that would violate the clear-sections canon (no extra zone between strip and card).
- **img-074-EmptyState** · ? — The first-run /today empty state (onboarding proposal A) is confirmed built in session memory ('Build onboarding proposal A: first-run /today empty state' completed). The dashboard.tsx line 467-473 mounts ClientsEmptyState as the first-run hero. The proposal asks to add an animated segmented-tick bar to this empty state — but (1) the empty state is already a real component and (2) the segmented-tick-bar is the same pattern requested by img-070 above. This is a duplicate of img-070 applied to the empty state container.
- **img-074-StatusRing** · ? — The session memory entry 'StatusRing progress-ring ADOPTED app-wide' confirms StatusRing is built. The workflow stage strip header (ObligationQueueDetailDrawer.tsx line 2663) shows a 'Workflow' header band. The existing PathToFilingSummary already renders stage state visually. The proposal asks to add a micro-segmented bar supplementing the stage-count label, but 'StatusRing' in the ALREADY-BUILT list covers the progress-ring pattern. Additionally, equal-width stage ticks would violate the 'asymmetric stage attention' canon rule (asymmetric stage attention feedback: workflow stages get UNEQUAL real estate).
- **img-077-daily-brief-card** · ? — The DailyBriefCard already implements inline citation chips in prose via CitationChip (daily-brief-card.tsx line 615) and BriefProse (line 579). BriefProse splits the AI text on [n] markers and renders each as a clickable accent pill ('tight accent pill, accent mono numeral' per comment at line 625). The chip is inline, tinted (bg-background-default + border-state-accent-border + text-text-accent), and resolves to obligationId. The proposal says 'each citation resolves to a tinted pill (obligation name + due countdown)' — the chip currently shows only the numeric ref [n], not the obligation name or DueCountdownText. However, the DashboardBriefCitation contract carries only obligationId, ref, and evidence (no obligationName or dueDate per packages/contracts/src/dashboard.ts lines 48-52). The data to expand the chip to 'name + due countdown' is fiction-blocked at the contract level.
- **img-079** · Button — The apply confirmation and undo affordance are already built as the DecisionBanners component (AlertDetailDrawer.tsx lines 561-661). When alert.status === 'applied', a DetailStatusBanner with tone='success' renders 'Applied to N clients · logged to audit ledger' + an Undo TextLink (line 632) when the 24h undo window is open. The Undo mutation is wired (line 1031). The 'View changes' link and 30-second countdown timer are absent from the current impl — View changes is new, and the countdown the proposal mentions would require a ticking timer component — but the core pattern (inline success + Undo button) already exists. The proposal asks for a 30-second countdown specifically, which is fiction: the server uses a 24-hour window (lines 596-604), not 30 seconds.
- **img-080** · ListRail — The filed/total usage bar is already built in apps/app/src/features/clients/ClientSummaryStrip.tsx lines 224-239. The comment at line 224 explicitly references '(img-080)'. It renders a h-1 rounded-full bar with bg-state-success-solid fill at (filedCount/totalObligations)\*100% width, plus a '{filedCount}/{totalObligations}' text label. This is the exact feature requested.
- **img-094** · iso-date-picker — IsoDatePicker (apps/app/src/components/primitives/iso-date-picker.tsx line 175) already uses a Popover with a full calendar grid including month/year navigation, selected date dark square rendering, and prev/next month buttons. The popover calendar IS the current implementation. The 'Original due' subtitle reference line is a minor addition but the core request ('pair with a popover calendar with selected date in dark rounded square + month/year nav') is already built.
- **img-103** · FilterTrigger (quick-filters dropdown on /deadlines) — `ObligationFiltersPopover` is confirmed built at `apps/app/src/routes/obligations.tsx` line 6120. It is a full faceted filter sheet with Status/Form/Client/State/Assignee/County/Saved-views tabs (`ObligationFilterTab` at line 6623), per-facet typeahead (`ObligationFacetSearchList`), staged Apply button showing result count, and AssigneeAvatar-style assignee chips. The proposal is an exact match of what already ships.
- **img-111** · audit transition row — `AuditLogTable` (`apps/app/src/features/audit/audit-log-table.tsx` lines 87–124) already groups events into day buckets with 'Today · Fri · Jun 5, 2026' + N-events band dividers rendered as sticky-feel header rows. The code comment at line 44 explicitly describes this: 'day-grouped timeline… Today / Yesterday get a relative prefix'. Exactly matches the proposal.
- **img-112** · workflow stage strip (active stage card) — `ActiveStageDetailCard` (`apps/app/src/features/obligations/queue/components/panels.tsx` line 1271) is already built with: stage title + status pill in the eyebrow, metadata pills (assignee, due date, sub-status), body description of what's required, a materials checklist drawn from `readinessChecklist` (real per-state data), and `StageActions` footer with primary action + implicit 'next stage' logic. The prop signature and rendering confirm this is the exact component the proposal describes.
- **img-115** · login page — `apps/app/src/routes/login.tsx` line 39 confirms: 'full-bleed two-column split — a product-story column (left) beside the sign-in card (right)'. The `ProductStory` component (line 398) renders static marketing copy + capability proof points on the left column, hidden below `lg`. The comment at line 46 explicitly notes 'The left column is a STATIC marketing proof — illustrative sample deadlines, not the visitor's live data.' The proposal is already shipped.
- **img-119** · FilterTrigger — ObligationActiveFilterChips is fully implemented in apps/app/src/routes/obligations.tsx (~line 6820) and rendered at ~line 3987. Each chip shows a dimension eyebrow + value + ✕ remove button, reads from URL params, covers status/date/form/client/state/assignee/county/dueWithin facets. /alerts does not have this chip row but the proposal says '/deadlines, /alerts, /clients' and the core pattern is already built for /deadlines.
- **img-121** · Obligations table (deadlines list) — ObligationStatusReadBadge (apps/app/src/features/obligations/status-control.tsx line 390-436) already renders a filled Badge using STATUS_VARIANT[status] (destructive/info/secondary/outline/success/warning) paired with StatusMark (the StatusRing icon). The DeadlineRow component (apps/app/src/features/obligations/queue/components/DeadlineRow.tsx line 304, 416) uses this badge in every table row. The SeverityChip primitive also exists. This is exactly the current pattern.
- **img-126** · FilterTrigger + multi-facet popover — ObligationFiltersPopover (apps/app/src/routes/obligations.tsx line 6120) is the existing two-panel faceted filter with tab strip, per-facet typeahead, per-facet count badges, staged Apply, and coverage of Status/Type/Assignee/Jurisdiction/Client/County/Condition/Saved-views — confirmed in the ALREADY-BUILT list above and the session memory (ObligationFiltersPopover in obligations.tsx ~line 6120).
- **img-128** · Workflow stage strip + ObligationTimeline — The Status tab already has a unified vertical layout: PathToFilingSummary (the 6-stage stepper/strip) at top inside a Workflow card, then ActiveStageDetailCard below it — both in the same section (ObligationQueueDetailDrawer.tsx lines 2629-2704). The ObligationTimeline is on a separate Audit tab (line 4791), which is intentional (tab-count-locked-at-4 canon: Status/Materials/Record/Audit). The proposal's 'today the two components are separate' is inaccurate for the stage-strip+stage-card, but the timeline-below-strip composition is a deliberate architectural decision, not a gap.
- **img-132** · FilterTrigger (client scope switcher on /deadlines) — Client filtering in /deadlines uses ObligationFiltersPopover which includes a client facet with typeahead search (apps/app/src/routes/obligations.tsx line 4934: searchPlaceholder='Search clients…', line 6310). ClientFilterOption extends FilterOption (line 588) and the client roster is loaded into clientOptions. Active client selections surface as ObligationActiveFilterChips chips below the toolbar. The 'colored avatar + entity-type chip' wrapper design differs cosmetically but the functional pattern (searchable client list + active chip display) is built.
- **img-144** · EmptyState on first-run /today (onboarding proposal A — ghost-card) — The first-run /today empty state is built and ships in dashboard.tsx lines 467–496. When clients=0 it renders `<ClientsEmptyState onImport={openWizard} />` (a single-hero import card with integration logo strip + CTA pair). When clients exist but no rules are active it renders a second-gap EmptyState pointing to /rules/library. The proposed 3-tile action grid (Add clients / Connect sources / Review rules) is a different visual form for the same states — not a missing feature. The existing single-hero pattern is the settled design. Task #26 in git log is marked completed: 'Build onboarding proposal A: first-run /today empty state'.
- **img-147-audit-transition-row** · ? — audit-log-table.tsx lines 187/262 already call `getAuditTimelineIcon(type)` which maps each AuditTimelineType (filing/amendment/decision/access/system) to a distinct Lucide icon rendered in a tinted tile. This IS the per-event-type icon prefix the proposal describes. The current taxonomy is coarser than the proposal's 'StatusRing for status_change / SparklesIcon for AI-read / ShieldCheck for filed' granularity, but per-event-type icons are already the pattern. Upgrading the icon set to finer granularity is a micro-polish on an already-built primitive, not net-new architecture.
- **img-151** · MonitoringChip — MonitoringChip.tsx is a static green LIVE chip with a tooltip (no segmented bar). AlertsListPage.tsx line 311-312 already derives `sourceErrorCount` from `sourceHealth.filter(s => s.healthStatus === 'degraded' || s.healthStatus === 'failing')` and shows a destructive error count badge separately (line 641-651). The proposal's 'segmented micro-bar within the chip (green healthy / amber degraded + counts)' is not built. However, the healthy vs degraded split data is available. BUT: this would mutate the MonitoringChip's chrome away from the settled 'calm green LIVE pill' design. The canon note (MonitoringChip.tsx line 72-75) records the deliberate 'borderless, quieter chip' decision. Upgrading the chip to show a bar changes its character from a calm status indicator to a diagnostic. This conflicts with the 'calm on dense surfaces' canon principle.
- **img-153-value-diff** · ? — value-diff.tsx lines 56-80 show that `ValueDiff` in `mode='compact'` already wraps itself in a Tooltip showing `label: from → to · delta`. The proposed 'tooltip bubble with the full old value, arrow, new value, plus delta on hover over the compact chip' is exactly what the compact mode provides. The component even has a `label` prop for the heading (e.g. 'Due date') and a `deltaClassName` for tone.
- **img-154** · Tooltip — tax-code-label.tsx lines 1-165 confirm both TaxCodeLabel and TaxCodeBadge already have full definition tooltips (label + jurisdiction + plain-English description via describeTaxCode). The TaxCodeTooltipBody renders label, jurisdiction eyebrow, and description. JurisdictionChip (state-badge.tsx line 103) also has a tooltip override prop that defaults to the full jurisdiction name. The 'definition tooltip on TaxCodeBadge / JurisdictionChip with plain-language explanation' is completely built.
- **img-164** · Affected-clients table (alert decision tool) — AffectedClientsTable.tsx (~line 440) already renders MatchStatusBadge which returns <Badge variant='secondary'>Already applied</Badge> for matchStatus==='already_applied' and <Badge variant='success'>Eligible</Badge> for eligible rows. The Apply/Applied state distinction is already communicated per row via this badge. The bulk-select + Apply button model is the intentional action pattern; a per-row Apply CTA pill would duplicate the existing action affordance.
- **img-178** · workflow stage strip — timeline.tsx line 140-146: MilestoneNode already renders border-l border-dashed vertical connector, solid accent dot for isCurrent (bg-state-accent-active), muted dot for isTouched, 0.45 opacity wrapper for !isTouched nodes. The dashed connector + accent active node + reduced opacity for past are all implemented.
- **img-178** · workflow stage strip — PathToFilingSummary (panels.tsx ~line 920) renders a horizontal solid border-t connector for entered stage edges (border-state-accent-solid) and a dotted border-t for future stage edges (border-dotted border-divider-regular), with solid-filled accent circles for done/active stages and empty border rings for upcoming. The node-connector visual grammar with accent active node and ghost upcoming rings is already implemented.
- **img-186** · audit transition row + scroll-spy anchors — AlertDetailDrawer.tsx lines 1720-1765: a sticky scroll-spy nav with 3 anchor items (Change/Source/Activity) and a framer-motion shared-layout underline that slides to the active section is already implemented. The core scroll-spy + active-indicator pattern is built. The proposal to make it a left vertical rail instead of a horizontal bar is a layout reskin of an existing pattern, not net-new functionality.
- **img-189** · MonitoringChip (LIVE) + source health — apps/app/src/features/rules/sources-tab.tsx lines 680-697: the source row already has a dedicated <TableCell> that renders relativeTimeShort(health.lastCheckedAt) inline (not in a tooltip — the tooltip on that cell carries the raw ISO stamp). A separate <TableCell> on line 688 renders the HealthBadge chip. Both are in-row columns, not hidden behind a detail pane. The proposal asks for 'last-crawl time inline with the row (not in a tooltip)' — that is exactly what is already rendered.
- **img-190** · StatusMark / StatusRing — apps/app/src/components/primitives/status-ring.tsx: StatusRing already renders a semantically distinct glyph per state — dashed ring (not_started), solid ring + pause bars (waiting), solid ring + slash (blocked), partial arc fill at 50%/85% (in_review/filed), solid disc + white check (completed). The session memory confirms 'StatusRing progress-ring ADOPTED app-wide (StatusMark, STATUS_ICON removed)'. Each state already gets a unique, non-generic SVG glyph.
- **img-196** · SeverityChip / StatusMark / StatusRing — apps/app/src/features/obligations/status-control.tsx lines 390-436: ObligationStatusReadBadge wraps StatusMark + label text inside a <Badge variant={STATUS_VARIANT[status]}>, where STATUS*VARIANT maps each of the 6 states to the appropriate variant (which carries bg-severity-*-tint and text-severity-\_ via the Badge primitive). The result is exactly the 'self-contained pill with ring icon + label' treatment described. apps/app/src/features/obligations/queue/components/DeadlineRow.tsx line 304 uses it in the status column.

## Fiction-blocked — no backend (26)

- **img-007** · workflow stage strip — No per-stage actor field exists in the schema. obligations table has a single assignee_id (one owner per obligation). The audit_log records who made each status transition but there is no 'who is currently working on stage X' query or data structure. packages/db/src/schema/audit.ts and packages/db/src/schema/obligations.ts confirm no stage-actor mapping. Rendering per-stage AssigneeAvatar dots would require net-new data: a stage_actors join table or last-actor-per-stage query not present in the backend.
- **img-034** · onboarding setup checklist (proposal B) — Onboarding proposal B (persistent setup checklist banner) is not built — memory explicitly states 'onboarding audit proposal A BUILT (first-run /today empty state), gaps B/C/D open.' No SetupChecklist, SetupBanner, or hasClients+hasSources+hasRules step-tile component was found anywhere in apps/app/src. The proposal applies a tile-grid layout to a component that does not exist yet; the underlying checklist is the missing prerequisite.
- **img-038** · workflow stage strip — AuditEventPublic (packages/contracts/src/audit.ts lines 71-89) has 'reason: z.string().nullable()' and 'action: z.string().min(1)' — both are freeform strings, not a structured enum. No 'triggerPhrase' field or 10-phrase enum exists on audit events. The 'Filed on time' / 'Extension filed' / 'Client signed off' outcome labels cited in the proposal cannot be derived as discrete structured values from the real audit schema; they appear only as hardcoded strings in static demo data (ObligationQueueDetailDrawer.tsx ~line 4419).
- **img-042** · Button (apply action), affected-clients table — The `PulseApplyOutput` schema in packages/contracts/src/pulse.ts (line 463-471) contains only `appliedCount` — there is no `updatedCount`, `skippedCount`, or `alreadyCurrent` field. The apply onSuccess handler at AlertDetailDrawer.tsx line 1062 uses only `result.appliedCount` in the toast, then calls `onClose()` immediately. The proposed 'Applied to 4 clients — 3 updated, 1 already current' breakdown requires fields that do not exist in the contract.
- **img-044** · daily-brief card — Two distinct proposals tagged to the same id: (1) cascade flow diagram — `AuditEventPublicSchema` in packages/contracts/src/audit.ts has no `cascade_id`, `parentId`, or any parent-child linking field; there is no mechanism to group events into a cascade tree. (2) 3-node daily-brief pipeline — packages/contracts/src/pulse.ts has no 'apply-ready count' or 'pending-decision count' aggregate; `PulseApplyOutput.appliedCount` is a post-apply result, not a pre-apply queue metric. Both are fiction-blocked.
- **img-065** · alert cards on /alerts list — No stacked-card grouping by source exists in AlertsListPage.tsx or AlertCard.tsx — the list renders flat rows (PulseAlertRow). More critically, the contract for alert list items (PulseAlertPublic in packages/contracts/src/pulse.ts) carries source_id but the server API returns a flat ordered list with no group-count or 'N unread from same source' aggregate. Rendering a stack requires a grouped query result that does not exist. Building the visual collapse/expand is also an interaction-design add outside normal scope.
- **img-075** · DetailSectionCard — The proposal asks for a fraction indicator (e.g. '1/4 complete') in each collapsed DetailSectionCard header, derived from 'real sub-tasks (StageTask items)'. However, StageTask items (apps/app/src/features/obligations/StageActions.tsx line 9) are UI-only task objects generated by useMemo in panels.tsx — they have no stored completion state. Their 'done' state is inferred from the obligation status transition, not from a per-task checked flag in the DB. There is no backend field that counts sub-tasks completed vs total per stage; only the terminal obligation status is stored. Fabricating a '1/4' fraction from UI-only task lists is fiction.
- **img-080** · ListRail — ClientPeekHoverCard.tsx only fetches orpc.clients.get and orpc.obligations.listByClient — audit events are not queried. The 'Recent activity (top 2 audit events)' mini-section requires a third query (orpc.obligations.getDetail or audit log) that is not present and would significantly expand the hover-card data footprint. The 'Open obligations top 3' portion is real (obligations array is available) but the two-mini-section shape requires audit data that is absent from the component.
- **img-082** · Segmented — actionMode (review_only vs due_date_overlay) is a server-derived field set at pulse ingestion — there is no mutation in packages/contracts/src/pulse.ts that lets a user switch an alert's actionMode at review time. AlertDetailDrawer.tsx (line 1471, 2058, 2081) already branches on actionMode as a fixed attribute, not a user choice. A Segmented toggle to switch modes would imply a mutation that does not exist.
- **img-084** · MonitoringChip — PulseSourceHealthSchema (packages/contracts/src/pulse.ts lines 311-335) has healthStatus (healthy/degraded/failing/paused), lastCheckedAt, nextCheckAt — but no 'currently crawling' boolean. There is no 'actively reading vs standing by' binary in the schema. The waveform's two states ('actively reading' vs 'idle') would require a real-time crawl-in-progress signal that does not exist in the backend.
- **img-089** · MorningSweepDialog / daily-brief-card — PulseAlertPublic (packages/contracts/src/pulse.ts lines 136-194) has no snooze field. Snooze exists only on obligation_instance (packages/contracts/src/obligation-instance.ts line 102: snoozedUntil). The 'Snoozed' mode in the 3-mode Segmented would have no backend data to filter on for alerts. The proposal's third mode is fiction-blocked.
- **img-095** · needs-attention-card / daily-brief-card — The dashboard API (contracts) returns topRows (individual obligation rows) and facets (aggregate buckets: overdue/today/next_7_days/next_30_days) — no per-calendar-day aggregation grouped by date. A month-grid where each date cell shows a dot colored by highest-severity status would require a new backend query returning {date: string, maxSeverity: string}[] which does not exist in the current contracts or orpc router.
- **img-099** · ListRail (AlertListRail / ObligationListRail) — AlertListRail and ObligationListRail render flat individual-item lists (one row per alert/deadline), not grouped-by-status sections with group headers. Right-justifying a CountPill 'on each ListRail row' for an individual alert row is meaningless (count would always be 1). Per-status group counts would require restructuring the rail into status-grouped sections — a new data shape and layout pattern not backed by the current query structure (alerts list returns a flat array, not grouped by status).
- **img-110** · rules review modal (AI draft section) — The `AiDraftReviewPanel` in `apps/app/src/features/rules/rule-detail-drawer.tsx` line 2002 has no instruction/prompt field — only a one-click 'Generate draft' button that fires `draftConcreteRule` mutation with no user-supplied intent. The proposal's specific preset tiles ('Accept with stricter threshold', 'Defer to next cycle') do not map to real mutations in `bulkVerifyCandidates` or `rejectCandidateMutation`. The instruction field itself does not exist in the backend contract for `draftConcreteRule`.
- **img-117** · affected-clients table — `PulseAffectedClient` schema (`packages/contracts/src/pulse.ts` lines 196–210) carries no `riskTier` or risk score field — it has `clientId`, `clientName`, `state`, `county`, `entityType`, `taxType`, `currentDueDate`, `newDueDate`, `status`, `matchStatus`, `reason`. The DB schema confirms no `risk_tier` column on the pulse-affected-client output. The only risk-profile reference in the codebase is an audit action name (`client.risk_profile.updated`) showing the field exists on the client entity, but it is not projected into `PulseAffectedClient` and would require a backend migration to expose.
- **img-120** · workflow stage strip — The apply in-flight state already uses ApplyingPill (a gradient-sweep pill, apps/app/src/features/alerts/components/ApplyingPill.tsx and DecisionActions.tsx line 56-62). The proposed step-progress checklist ('Reading change / Identifying affected clients / Drafting rule update / Ready to confirm') with ETA labels requires discrete server-side mutation step signals that do not exist in the contract (PulseListAlertsInputSchema, pulse.ts); the apply mutation is a single call with no step-granularity stream. 'Projected ETA' is also explicitly banned fiction.
- **img-135** · StatBand + Segmented control (on /alerts history or /audit-log) — AlertHistoryView.tsx (line 227) already has StatBand but no time-range Segmented. PulseListHistoryInputSchema (packages/contracts/src/pulse.ts line 295-302) has only limit/status/cursor — no createdAfter/createdBefore/dateWindow parameter. The backend has no date-windowed count endpoint for handled/applied/dismissed/unactioned. A UI Segmented with no backing query filter would show stale all-time counts regardless of the selection.
- **img-156** · Tooltip / EmptyState — SparklesIcon appears in PulseAlertRow (silent aria-hidden icon, no Tooltip), MorningSweepDialog (decorative), AlertConfidencePill (standalone chip). None has a rich popover. The proposal's 'See how it works' TextLink requires a real docs URL or a navigable MorningSweepDialog flow — there is no docs URL in the codebase and MorningSweepDialog is gated to AI-run sweep data. The 'mini-illustration of the AI-read flow (before→after arrow)' would be a bespoke SVG/illustration — no illustration system exists in the app (no illustration-style primitive). The 'See how it works' link is fiction unless a real destination URL exists.
- **img-159** · Audit transition row (bqUOC anatomy) — The audit event contract (contracts/src/audit.ts) has `reason: string | null` and `beforeJson: unknown | null` / `afterJson: unknown | null` — NO typed `from_status`, `to_status`, or `triggered_by` (rule id/name) fields. The structured-sentence chip pattern ('Status changed from [blocked] to [in_review] · triggered by [Rule #12]') requires reliable typed from/to/triggered_by values. Parsing them out of `beforeJson`/`afterJson` blobs is brittle and the contract does not guarantee the shape. The current audit-log-table already renders a text `changeHeadline` built by `buildAuditChangeView` from those blobs — adding linked TaxCodeBadge-style chips for the same data requires contract changes to expose typed before/after status + rule reference as first-class fields.
- **img-161** · Daily brief card / actions-list — DashboardBriefPublicSchema (packages/contracts/src/dashboard.ts line 192) returns only {text: string|null, citations, status, generatedAt}. No structured suggestedActions array exists. The brief text is a free-form AI sentence — parsing it into 2-3 typed TextLink rows (SparklesIcon + verb + object + href) is fabrication. The MEMORY canon entry project_marketing_landing_active_loop.md explicitly notes 'suggested-actions is just a next-step card' as a positioning note, not an implemented backend feature.
- **img-170** · affected-clients table — PulseAffectedClientSchema (packages/contracts/src/pulse.ts line 196) has no confidence field — confidence (z.number().min(0).max(1)) is on PulseAlertPublicSchema (line 158), i.e. per-alert not per-client. Rendering the same alert-level confidence as a 'per-client row' score (e.g., '84%') is misleading fiction: every row in the table would show the identical number regardless of which client it is.
- **img-171** · clients list table — clientsContract.listByFirm (packages/contracts/src/clients.ts line 449) returns ClientPublicSchema which contains no per-client obligation counts (no overdueCount, blockedCount, or obligationCount field). Deriving risk exposure from 'count of overdue/blocked obligations per client' requires a backend join that does not exist in the contract. Building this would require a new endpoint or extending the list payload.
- **img-182** · daily-brief card — No monthly historical obligation data endpoint exists in any contract (packages/contracts/src/). The dashboard.ts DashboardLoadOutput returns only current-window aggregates (topRows, triageTabs, summary). The workload.ts contract covers a rolling 7-30 day window, not a 12-month status-bucketed history. A 12-month dot-column sparkline keyed on obligation status at time of filing requires a new time-series query with no existing backing endpoint.
- **img-188** · priorities table + daily-brief card — No suggestedActions or nextSteps array exists in DashboardLoadOutputSchema (packages/contracts/src/dashboard.ts). The brief contract returns only a free-form text string. Building 2-4 AI-generated next-step cards with ALERT/DEADLINE/RULE eyebrows and typed CTA buttons requires a new structured AI pipeline endpoint that does not exist. Deriving these heuristically from obligation/alert counts is a different feature than 'AI-generated next-step cards' as specified.
- **img-199** · ToggleChip (onboarding classification) — packages/contracts/src/firms.ts FirmCreateInputSchema (line 126) has no entity_type or obligation_type field — only name, timezone, internalDeadlineOffsetDays, monitoringStartDate. Entity types (CLIENT_ENTITY_TYPES in packages/db/src/schema/clients.ts line 240) live on per-client records, not on the firm. No server mutation activates obligation types based on a firm-level entity classification. A ToggleChip grid for 'Individual / Partnership / Trust / Estate / Corporate' at onboarding step 1 has no field to write to.
- **img-200** · DetailSectionCard (settings / client profile sections) — The proposal's central affordance is 'pencil-edit icon appearing on hover' for client.name, jurisdiction, entity type, and fiscal year. packages/contracts/src/clients.ts lists no clients.updateName mutation (confirmed: no ClientUpdateNameSchema). Jurisdiction and entity type changes go through multi-step impact-preview dialogs (previewClassificationRecompute / applyClassificationRecompute), not inline pencil-edit. The hover-edit pattern for these fields has no supporting mutation.

## Canon-reject (21)

- **img-001** · priorities table (column layout) — The asymmetric-stage-attention canon explicitly applies to the workflow stage strip (filing stages), not to the priorities table layout. MergedBriefCard in apps/app/src/features/dashboard/merged-brief-card.tsx uses a flat Table with a bucket tab selector (week/month/overdue) — asymmetric column widths per urgency tier would violate the 'calm on dense surfaces' feedback and the one-purpose-per-panel rule (the bucket Segmented already conveys urgency tier distinction). The canon says 'active ~40-50%, inactive ~10%' for the STRIP, not for a table surface.
- **img-002** · daily-brief card — DailyBriefCard (apps/app/src/features/dashboard/daily-brief-card.tsx lines 56-70) explicitly documents the 2026-06-15 Yuqi decision to CUT workload counts and recap lines: 'the workload-counts line and the since-last-visit recap were both cut.' The daily-brief workload counts canon entry in MEMORY.md also says 'daily-brief workload counts deliberately CUT.' Adding multi-zone per-channel CountPills (alerts/deadlines/rules) reintroduces what was intentionally removed — direct canon violation.
- **img-014** · Priorities table — The priorities table already has ROWS_PER_BUCKET = 5 cap (merged-brief-card.tsx line 56) and a 'See all N deadlines' footer link — the overflow is already handled. The 'telescoping' compact-row variant (32px thin rows vs full-height top rows) is an asymmetric density treatment on a TABLE, which conflicts with the 'calm on dense surfaces' feedback and the 'demote-don't-delete' canon (collapsing lower rows to name-only loses the client and due-date columns, the decision-critical facts). The row cap is already built; the variable-height-per-row treatment is a canon violation.
- **img-019** · StatBand — The memory note 'StatBand card summary — one shared summary band across 5 surfaces (clients list+detail…)' and the StatBand color-budget doc (stat-band.tsx lines 43-52) both codify that cells are deliberately uniform. The /clients/[id] surface uses ClientSummaryStrip (confirmed in ClientDetailWorkspace.tsx line 792) which already surfaces 'Next due' as one of five equal text-lg cells. Promoting one cell to a dominant large stat while compressing others to text-xs is the explicitly-rejected asymmetric-size pattern.
- **img-035** · DetailSectionCard grid on client detail — The 'clear sections, not boxes' canon (feedback_clear_sections_not_boxes.md in memory) explicitly bans bordered-card-per-section patterns: 'delineate with section headers + full-width rules + whitespace, not a bordered card per section; reserve boxes for tables/buttons.' A 2-col grid of compact labeled tiles — one bordered card per field (Entity type, Filing status, Jurisdiction, Assignee, Notes) — is exactly the banned pattern.
- **img-037** · StatBand — The StatBand color-budget doc (apps/app/src/components/patterns/stat-band.tsx lines 43-52) and the memory note 'StatBand cells deliberately uniform' both prohibit asymmetric cell backgrounds. The proposal breaks the uniform surface contract by making one cell dark navy while neighbors stay light — the exact pattern the color-budget rule ('if every column is colored, none stands out') guards against via the inverse: if one cell is dramatically darker, it overrides every other signal on the band.
- **img-039** · daily-brief card — The daily-brief-card.tsx doc comment (lines 58-65) and inline comment (lines 270-273) both record that the workload-counts line (overdueCount, dueThisWeekCount) was explicitly cut by Yuqi on 2026-06-15 because it duplicated the Priorities buckets. The alerts + overdue pills were further removed 2026-06-18. The proposal re-introduces overdue count and due-today count as icon-prefixed line items — same information with additional chrome. The 'daily-brief workload counts deliberately CUT' is a canon rule in the project memory.
- **img-046** · affected-clients table — The 'one purpose per panel' canon (project memory reference_close_interaction_policy / feedback_one_purpose_per_panel.md) prohibits panels doing two things. The AffectedClientsTable is the apply-decision workspace; adding a List|Cards view toggle gives it two distinct presentation modes for the same data set. The existing table variant='apply'/'review' already serves two discrete use-cases; a third mode for aesthetics violates the single-purpose rule. The table itself is in apps/app/src/features/alerts/components/AffectedClientsTable.tsx.
- **img-090** · StatBand — The client detail page uses ClientSummaryStrip (not StatBand), and the /clients list StatBand already has the 'At risk' column with conditional sub text ('need attention' / 'on track', ClientFactsWorkspace.tsx line 1414). Adding a cross-band natural-language sentence violates the StatBand-cells-deliberately-uniform canon — the MEMORY.md StatBand card summary entry explicitly calls cells 'uniform'. The data is already conveyed per-cell.
- **img-096** · AlertListRail + AlertDetailDrawer — AlertListRail.tsx line 304-308 explicitly comments: 'The relative-time third line is gone entirely (batch 4 #9): in a date-sorted rail it restated the date column's information as noise.' This was a deliberate design removal, not an oversight. Reinstating RelativeTime below the title on rail items directly contradicts the recorded design decision. RelativeTime primitive exists but was intentionally excluded from this surface.
- **img-100** · Workflow stage strip (6-stage obligation strip) — The 6-stage workflow strip is already built as a horizontal `grid-cols-6` `PathToFilingSummary` component (`apps/app/src/features/obligations/queue/components/panels.tsx` line 853). The asymmetric-stage-attention canon explicitly mandates the active stage gets 40–50% real estate via the companion `ActiveStageDetailCard` — not a compact equal-height vertical list. Replacing the horizontal strip + active-card with a compact vertical left-bar would violate the asymmetric-real-estate canon.
- **img-101** · Affected-clients table in AlertDetailDrawer — `PulseAffectedClient` carries `taxType: z.string().min(1)` (contracts/src/pulse.ts line 203), so the data exists. However, the inline comment in `AffectedClientsTable.tsx` line 177 explicitly documents the deliberate decision: 'Entity + Location replace the per-row Form (the alert already names the form)'. Re-adding a form-type chip column contradicts the demote-don't-delete and one-home-per-fact canon — the form is already named in the alert title, so a per-row chip is a duplicate signal.
- **img-104** · ValueDiff primitive — Two items share this id. (1) Inline stat-chip in the daily brief: `DailyBriefCard` (`apps/app/src/features/dashboard/daily-brief-card.tsx`) was explicitly de-densified 2026-06-15 — the workload-counts line was cut because it duplicated the Priorities buckets below (one-home-per-fact canon). Re-embedding count metrics as dark stat-chips reverses that cut. (2) Sources-used chip strip in AlertDetailDrawer: `PulseDetail` carries a single `source` (displayed via `AlertSourceLink` at drawer line 1641) — no multi-source attribution array exists on the alert contract, making this fiction-blocked.
- **img-109** · daily-brief-card — `DailyBriefCard` (`apps/app/src/features/dashboard/daily-brief-card.tsx`) already has `BriefActionPills` at the bottom providing the action row. A two-zone left-fact / right-action layout with a 'Review alerts' button would duplicate the Needs-attention section directly around this card (same count, same destination, within ~150px) — violates one-home-per-fact canon. The 2026-06-15 de-densification already cut Alerts and Overdue pills from this card explicitly for this reason.
- **img-122** · Daily-brief card — The MEMORY canon 'daily-brief workload counts deliberately CUT' is in reference_stat_band_card_summary and project_stat_band_card_summary: 'daily-brief workload counts deliberately CUT'. The merged-brief-card.tsx is the 'Priorities' queue with overdue/week/month buckets — it has no 'completed today' counter on the backend (PulseListAlertsInputSchema has no completed-today field; ObligationInstancePublic dueDate is a date not a completion-today flag). Adding a progress bar re-introduces the cut workload-counts pattern.
- **img-130** · ObligationTimeline (milestone groups) — ObligationTimeline (apps/app/src/features/obligations/timeline.tsx) already groups events into milestone buckets via MILESTONE_MAP (line 27) and renders MilestoneNode per LIFECYCLE_V2_STATUSES. The proposal to make these collapsible by default (with the active one open) violates the 'collapsible density' canon: 'smart default expand-state by value' — all milestones already render collapsed to just a header+circle when empty (isTouched=false renders at opacity-45 with no event cards). Adding an explicit chevron collapse/expand on non-empty past milestones is reasonable, but the description says 'collapsed by default' for ALL past milestones — this collapses decision information the CPA needs to scan ('demote, don't delete'; 'collapsible density' canon says past/completed sections may collapse, but grouping all past into hidden-by-default risks hiding the audit trail purpose of this tab).
- **img-140** · Rules accept bulk-review modal — obligation scope selector — BulkReviewListModal (rules.library.tsx ~line 4656) goes straight to the affected-clients table with include/exclude checkboxes. The proposed 2×2 scope-selector tile step (Apply to all eligible / Apply to confirmed only / Skip and flag / Reject all) has no precedent in the codebase. More critically, the 'one purpose per panel' canon explicitly bans multi-step wizard gates before the table when the table itself is the action surface — the modal's include/exclude row mechanic already surfaces per-rule scope decisions without a pre-gate. A pre-step radio-card grid duplicates the existing per-row checkbox selection in a less precise, higher-friction form.
- **img-147-detail-section-card** · ? — AlertDetailDrawer.tsx uses scroll-spy section navigation (lines 1537–1551) per the alert-detail-decision-tool canon memory: 'Change/Source/Activity are scroll-spy anchors NOT tabs (Pencil xcTFK).' The canon doc explicitly established this as the settled decision. The proposed persistent right-rail (always-co-visible notes + recent audit) contradicts the one-purpose-per-panel canon and would introduce a two-column layout the Pencil file does not specify. The decision to use scroll-spy over a persistent split is already recorded in project_alert_detail_decision_tool.md.
- **img-150** · EmptyState — \_RulesLibraryEmptyState (rules.library.tsx line 3185) is the settled empty state: canonical EmptyState primitive with icon+title+description+CTA pair (Import from sources / New rule). The proposal adds 3-4 'action starter' tiles with mini wireframe previews when fewer than 3 rules exist. This violates the 'clear sections not boxes' canon: the proposal replaces a clean EmptyState primitive with a grid of bordered preview cards that adds surface noise. The existing CTA already routes to the same actions (sources import, new rule). Adding the tile grid is pure decoration over a settled pattern.
- **img-167** · Daily brief card — daily-brief-card.tsx TodayLine (line 448) renders the AI sentence at text-base/text-text-primary — already the visual lead of the card body. Bumping to 18-20px/500 weight would violate the type weight restraint canon (MEMORY: feedback_type_weight_restraint.md — 400 default, 500 key data, 600 page+section titles only; urgency gets SIZE not weight+color). The card already has a collapse/expand model with the teaser as 'above-the-fold'; adding a separate 'Read more' trigger beneath a hero sentence duplicates the existing collapse affordance.
- **img-191** · priorities table — The today responsive contract (reference_today_responsive.md memory) explicitly mandates 'overflow-x-auto frame (never clip columns)' for the priorities table. Collapsing STATUS + DUE into the name cell at narrow breakpoints directly contradicts this contract. apps/app/src/features/dashboard/merged-brief-card.tsx line 402 implements this as 'overflow-x-auto rounded-xl … xl:overflow-y-auto' — scroll-sideways is the deliberate responsive choice, not column collapse.

## Duplicate (4)

- **img-012** · EmptyState (ghost-card visual) — This id appears twice in the batch with two different ideas. The row-cap idea (hard cap + Show N more) overlaps with img-014 which proposes the same telescoping/cap concept. The first-run sticky-note-wall illustration idea is net-new but is a pure illustration asset request with no data dependency — not a product/UX pattern. Treating the two sub-ideas as covered: the cap is a duplicate of img-014's concept, and the illustration is too thin to be a standalone buildable opp (no component, no data, just an image drop into the existing ClientsEmptyState).
- **img-118** · FilterTrigger (column picker / quick filters dropdown) — `ObligationFiltersPopover` (`apps/app/src/routes/obligations.tsx` line 6120) already has per-tab selected-count badges via `ObligationFilterTab` (line 6623) — each tab shows the active-selection count in a pill. The 'N/M active/total' fraction format is a minor cosmetic variant of the existing active-count badge. The filter sheet this proposal describes is already the ObligationFiltersPopover (marked already-built in img-103). The grouped-tree-with-counts pattern is a duplicate of that existing sheet's tab strip.
- **img-158** · StatusBanner / first-run hint strip — This is the same first-run /today empty state as img-144 (proposal A), applied to the description text instead of the tile grid. dashboard.tsx already renders `<ClientsEmptyState>` for the zero-clients first-run state (a purpose-built settled hero). There is no 'ShortcutNudge' primitive in the codebase — grep finds zero results for that term. The proposal to replace prose with dismissable nudge pills is a variant of the already-settled img-144 first-run state, and 'ShortcutNudge' as a named primitive does not exist.
- **img-181** · affected-clients table — Identical proposal to img-164: both describe adding an 'Apply' accent pill (active) / muted 'Applied' pill (done) per row in the affected-clients table to replace or augment the bulk-select model. img-164 already adjudicated as already-built.
