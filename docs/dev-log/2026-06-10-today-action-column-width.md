# Today Action Column Width

- Added a small preferred width to the `/today` Priority Actions action column
  with `w-[400px]`.
- Kept the existing hover treatment unchanged: prompt text still uses
  `group-hover:font-medium group-hover:text-text-primary`.
- Updated the table style note so the documented column recipe matches the
  implementation.
