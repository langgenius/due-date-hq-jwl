# i18n catalogs — UX copy series closeout (2026-06-11)

Regenerated en + zh-CN catalogs against the committed tree (parallel-session WIP set aside via pathspec stash so `extract --clean` saw only committed sources), with zh-CN translations filled for every msgid introduced by the copy-audit batches 1–15. 3,988 messages, 0 missing, `compile --strict` clean — keeps the i18n-catalog-drift workflow green.
