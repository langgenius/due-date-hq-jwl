# 2026-05-27 8879 Routing Copy

## Context

Browser feedback on the deadline summary drawer asked what `Pre-stage 8879
packet for client` meant. Follow-up feedback pointed out that the product does
not currently send or collect client e-file authorization, so an Evidence-only
8879 routing task creates a dead-end workflow.

## Changes

- Removed the 8879 routing task from the In Review action list.
- Kept `Approve return`, `Leave note for preparer`, and `Mark return submitted
to authority` as the visible actions that match current product behavior.
- Updated the related design and dev-log references to treat Form 8879 client
  authorization as deferred until there is a real workflow.

## Verification

- `pnpm --filter @duedatehq/app i18n:extract`
- `pnpm --filter @duedatehq/app i18n:compile`
- `pnpm --filter @duedatehq/app test -- src/routes/obligations.test.ts`
- `pnpm check` (passes; existing 6 warnings unchanged)
- Browser check on `http://localhost:5173/deadlines/000000000005/summary`:
  the In Review action list no longer shows a 8879 Evidence-routing task.
