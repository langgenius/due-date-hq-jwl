# Daily Brief — "Failed" matches the message colour

Date: 2026-06-08

Yuqi: the "Failed" label in the thin failed banner should be the same text
colour as "We couldn't generate today's brief." The message is
`text-text-tertiary`; the label was `text-text-secondary`. Set the label to
`text-text-tertiary` so they read as one quiet line.
