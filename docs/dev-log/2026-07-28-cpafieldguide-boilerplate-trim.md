# cpafieldguide: methodology boilerplate trimmed to a strip; deadline category bridges to DueDateHQ

2026-07-28 (closes the dropped tier-2 items from the morning's indexing diagnosis)

- The full 4-part "How this guide works" block (~300 identical words) now renders on the
  homepage only (anchored `#how-this-guide-works`); the other 60+ pages carry a one-line
  trust strip linking to it. Raises unique-content ratio on exactly the pages GSC shows as
  "Discovered – currently not indexed" (22).
- `/deadline-monitoring-software` now cross-links the category definition on DueDateHQ
  (`/what-is-deadline-monitoring`) — the monitoring-vs-tracking distinction the category is
  organized around. Entity + topical bridge between the two properties.

Regenerated: strip on tool/vs/alternatives/guide/category pages, full block + anchor on
home, cross-link live in the deadline category. Deployed via `npx vercel --prod`.
